# CLAUDE.md

Diese Datei dokumentiert das Projekt-Setup von **stream-hub** für zukünftige Arbeit mit Claude Code.

## Projekt-Zweck

Streaming Overlays & Serverless APIs für Twitch/Streamlabs, gehostet auf Vercel. Die Overlays werden als Browser-Quellen in OBS (o.ä.) eingebunden und zeigen Live-Daten wie Rocket-League-Session-Stats, Follower-Fortschritt mit Party-Animationen sowie einen Twitch-Chat-Feed.

## Dateistruktur

- **`index.html`** — Session Stats Widget (Wins, Losses, Winrate). Fragt periodisch (alle 20s) die Vercel-API `/api/stats` ab und berechnet die Session-Differenz gegenüber dem beim Laden erfassten Basiswert. Zeigt zusätzlich einen Live/Cached/Offline-Statuspunkt basierend auf den `cached`/`stale`-Feldern der API-Antwort, sowie ein **Rang/MMR-HUD** (Icon, Tier-Name, MMR-Zahl), sobald `/api/stats` ein `rank`-Objekt liefert — bleibt bei `rank: null` (z.B. unranked) einfach unsichtbar statt kaputte Werte zu zeigen. Nutzt noch den ursprünglichen Glassmorphism-Look (siehe Styling-Guides) — wurde bei den Hood/Ghetto-Redesign-Runden bewusst nicht mit umgestellt.
- **`goal.html`** — Follower-Ziel-Widget mit Fortschrittsbalken **plus komplettes Alert-/Audio-/Physik-System**, im "Urban Blackout"-Look (siehe Styling-Guides). Verbindet sich per nativem Browser-`WebSocket` direkt mit **Twitch EventSub** (`wss://eventsub.wss.twitch.tv/ws`, keine Drittanbieter-Client-Library nötig) und meldet die benötigten Event-Subscriptions (Follow, Sub, Resub, Bits, Raid) über `/api/twitch-eventsub` an, sobald die Session-ID vom WebSocket-Handshake vorliegt. Eigene Reconnect-Logik mit Backoff, Behandlung von Twitchs `session_reconnect`-Handoff. Bei Events wird ein Party-Popup über eine Warteschlange (`popupQueue`) angezeigt (mehrere Events hintereinander spielen sauber nacheinander ab). Struktur: `#party-popup` (äußere, ungeclippte Hülle — trägt Position/Opacity/Transform inkl. Lowrider-Hydraulik-Physik + Recoil-Kick) umschließt `#party-panel` (die eigentliche geclippte "Panzerplatte" mit zerklüftetem `clip-path`, Einschusslöchern, Riss-Overlay) sowie als Geschwister-Elemente `.most-wanted-badge`, `.license-plate` und zwei `.crime-tape`-Streifen, die absichtlich über den Panel-Rand hinausragen (deshalb nicht Kind von `#party-panel`). FX-Layer: `#spray-canvas` (Partikel-System `SpraySystem`, noise-gesteuert, inkl. dynamischer Bullet-Hole-Risse), `#impact-flash` (kontinuierlicher sub-bass-gesteuerter Flash), `#muzzle-flash` (einmaliger Blitz beim Alert-Trigger), `#cop-siren` (rot-blaues Sirenen-Strobe während Alerts), `#scanline-overlay` (VHS-Scanlines, immer oben). Eigene `AudioAnalysisRig`-Klasse zerlegt jeden Alert-Sound per BiquadFilter in Sub-Bass/Mid-High-Bänder und treibt darüber Hydraulik-Hop, Liquid-Bar-Impulse und Spray-Nachschub. Zusätzlich läuft ein **Idle-Ambient-Layer** (`startIdleAmbient`/`emitIdleSpark`) permanent im Hintergrund — unabhängig von Alerts wird alle 700–1200ms ein einzelner Funke an zufälliger Position emittiert, damit der Screen in den >95% der Sendezeit ohne Event nicht komplett statisch wirkt. Die Sounds (`follow.mp3`, `sub.mp3`, `bits.mp3`, `raid.mp3` aus `/assets/audio/`) werden per Web Audio API vorab dekodiert; fehlt eine Datei, bleibt nur der jeweilige Alert stumm. `?test=follow|sub|resub|bits|raid|goal` löst einen Alert manuell aus, ohne auf ein echtes Twitch-Event zu warten. **GTA-Wanted-Level-Widget** (oben links, Geschwister des Follower-Widgets): 5 Sterne füllen sich über Subs/Bits (1 Sub = 1 Stern, 100 Bits = 1 Stern). Zustand liegt bewusst **nur im Browser** (`localStorage`, Key `dangsxr_wanted_points`) — keine externe Datenbank, kein Server-Roundtrip; überlebt Reloads derselben Browser-Quelle, ist aber nicht geräteübergreifend synchron (bei einem einzelnen Streamlabs-Mobile-Tablet kein praktischer Nachteil). Bei 5/5 feuert ein eigener `wanted`-Alert-Eintrag (Mündungsfeuer/Sirene/Spray wie jeder andere Alert). **Chat-Commands** über eine zweite, unabhängige `tmi.js`-Verbindung (getrennt von `chat.html`, in try/catch gekapselt, damit ein CDN-Ausfall nicht den Rest des Bootstraps mitreißt): `!shoot` (offen für alle, spawnt ein Bullet-Hole, 2.5s-Cooldown) sowie `!siren`/`!tor`/`!fail` (Broadcaster/Mod-only via Twitch-Badge-Tags — `!tor`/`!fail` sind der bewusste Ersatz für einen automatischen Rocket-League-Tor-Detektor, der technisch nicht machbar ist, da das Overlay als Browser-Quelle keinen Zugriff auf Bild/Ton des Cloud-Gaming-Streams darunter hat). **Soundtrack-Badge** (unten mittig, Laufschrift): Text kommt aus dem `?track=`-URL-Parameter (z.B. `goal.html?track=Soundtrack%20by%20Twitch`, gleiches Prinzip wie `chat.html`s `?channel=`), fällt ohne Parameter auf einen Default-Text zurück; rendert per `innerText` (kein XSS-Risiko über den Parameter möglich). **Fehlerkorrektur: echte Follower-Zahl statt Platzhalter (aktuell)**: Nutzer meldete, das Follower-Ziel-Widget könne "nicht mit meinem Twitch-Account verbunden sein", da sein echter Stand (210) nicht zum angezeigten Wert passte — Ursache gefunden: `currentFollowers` war seit jeher als literale Konstante `209` im Code hinterlegt (nie live von Twitch abgefragt), erhöhte sich seitdem nur noch lokal pro während der jeweiligen Seiten-Sitzung empfangenem `channel.follow`-EventSub-Event und sprang bei jedem Neuladen der Seite zurück auf diesen Fake-Ausgangswert. Neue `api/twitch-followers.js` (siehe oben) holt jetzt beim Laden einmalig die echte Gesamt-Follower-Zahl über Twitch Helix; `currentFollowers` wird direkt danach überschrieben, bevor die UI zum ersten Mal rendert (`syncRealFollowerCount()` statt direktem `updateUI()`-Aufruf im Bootstrap). Schlägt der Call fehl, bleibt der alte Platzhalter als Fallback stehen statt die Seite kaputt zu zeigen. Identischer Fix auch in `overlay.html` nachgezogen (dort dupliziert, `updateGoalUI()`/`syncRealFollowerCount()`). Getestet: Playwright bestätigt sowohl den Erfolgsfall (gemockte API-Antwort mit `total:245` erscheint korrekt in der UI) als auch den Fehlerfall (abgebrochener Request führt zu sauberem Fallback statt kaputter/leerer Anzeige), in beiden Dateien.
- **`chat.html`** — Chat-Overlay Widget im "Urban Blackout"-Look, physik-basierte fallende/abprallende Karten (Gravitation/Bounce/Friction, eigener rAF-Loop, selbstterminierend). Rendert Twitch-Emotes (`tags.emotes`) direkt als Bilder vom Twitch-CDN, XSS-sicheres Escaping. **Kanal kommt ausschließlich aus dem `?channel=`-URL-Parameter** — es gibt keinen hartkodierten Kanalnamen mehr. ⚠️ **Wichtig für die OBS-/Streamlabs-Browser-Quellen-URL: `chat.html?channel=dangsxr1000` (mit Parameter!), sonst läuft automatisch der Demo-Modus statt des echten Chats.** Fehlt der Parameter, wird **nie** weitergeleitet — stattdessen startet automatisch ein Demo-Modus (`startDemoMode()`), der alle 3.5s eine von sieben Beispielnachrichten aus `DEMO_MESSAGES` zeigt, pausiert über die Page Visibility API. **Fehlerkorrektur: tmi.js Multi-CDN-Fallback (aktuell)**: Nutzer bestätigte live einen "Fehler"-Status bei `chat-vibes.html` und meldete, dass echter Twitch-Chat generell nicht ankommt — Verdacht: alle 6 Dateien, die `tmi.js` nutzen (`chat.html`, `chat-vibes.html`, `avatar.html`, `goal.html`, `overlay.html`, `in-game.html`), luden die Library bisher ausschließlich über einen einzigen fest verdrahteten `<script src="cdn.jsdelivr.net/...">`-Tag; schlägt genau dieser eine CDN-Request in der Streamlabs-Mobile-Android-WebView fehl, bleibt der komplette Chat (und darüber gekoppelte Funktionen wie Chat-Commands und die Avatar-/Bär-Reaktion in `overlay.html`/`in-game.html`) tot — plausible Erklärung auch für die zuvor gemeldete "Bär macht garnichts"-Beobachtung. Neue gemeinsame Datei **`assets/js/tmi-loader.js`** (`window.loadTmiWithFallback(onReady, onFailed)`) versucht `tmi.js` nacheinander von zwei CDNs (`cdn.jsdelivr.net`, dann `unpkg.com` als Fallback bei Ladefehler); alle 6 Dateien binden jetzt diesen Loader statt des direkten CDN-`<script>`-Tags ein und wickeln ihren bestehenden `tmi.Client`-Bootstrap unverändert in `loadTmiWithFallback(...)` ein. `chat.html` bekam dabei zusätzlich ein bisher fehlendes try/catch samt sichtbarer Fehlermeldung im Chat-Feed selbst (vorher: unbehandelter `ReferenceError` bei fehlendem `tmi`, Seite blieb stumm auf "Verbinde mit #channel ..." hängen). Aus der Sandbox nicht gegen echte CDN-Erreichbarkeit testbar (beide Domains von der Netzwerk-Policy blockiert) — Playwright bestätigt aber die Fallback-Mechanik selbst: bei blockiertem jsdelivr wird automatisch ein zweiter Request an unpkg.com ausgelöst, alle 6 Dateien laden weiterhin ohne JS-Fehler, egal ob beide CDNs erreichbar sind oder nicht. **Echter Root-Cause gefunden (aktuell)**: Nutzer bestätigte live per Screenshot weiterhin "CDN-Fehler" bei `chat-vibes.html` — trotz des gerade ausgelieferten Multi-CDN-Fallbacks. Diagnose per `npm view`/`npm pack tmi.js@1.8.5` (Sandbox kann npm-Registry erreichen, auch wenn beliebige CDN-Domains blockiert sind): das npm-Paket `tmi.js@1.8.5` listet in seiner eigenen `package.json` unter `"files"` nur `["lib", "index.js", "LICENSE"]` — **kein gebauter Browser-Bundle wird überhaupt veröffentlicht**, nur roher Node-CommonJS-Quellcode. Der `tmi.min.js`, den `tmi.js` selbst per `npm run build` erzeugt (`build:browserify`/`build:uglify`-Skripte), landet nur lokal in einem `build/`-Ordner, der laut `"files"`-Feld nie in den npm-Tarball gepackt und folglich auch nie von jsdelivr/unpkg ausgeliefert wird (beide CDNs spiegeln exakt den npm-Tarball). Die bisher verwendeten Pfade (`.../185/tmi.min.js`) haben dadurch **vermutlich noch nie in der Geschichte des Projekts funktioniert** — das erklärt rückwirkend jede bisherige "Chat kommt nicht an"/"Bär reagiert nicht"-Meldung, unabhängig von WebView, Netzwerk oder CDN-Wahl. Fix: Browser-Bundle einmalig selbst gebaut — `browserify`+`babelify` (exakte Versionen aus `tmi.js`s eigenen `devDependencies`, exakt nach dessen `build:browserify`-Skript) gegen `index.js` des Pakets, danach mit `uglify-js` minifiziert (`build:uglify`-Skript) — Ergebnis als **`assets/js/tmi.min.js`** (51KB, MIT-lizenziert, `tmi.min.js.LICENSE` liegt daneben) im Repo abgelegt. `tmi-loader.js` versucht jetzt zuerst diese lokale, same-origin-Datei (keine externe Netzwerk-Abhängigkeit mehr im Normalfall), die beiden alten CDN-URLs bleiben nur noch als Notfall-Fallback falls die lokale Datei je fehlen sollte. Playwright bestätigt: mit blockierten `cdn.jsdelivr.net`/`unpkg.com`-Domains lädt `tmi`/`tmi.Client` in allen 6 Dateien trotzdem erfolgreich über die lokale Datei, `new tmi.Client(...)` lässt sich instanziieren, `.on()`/`.connect()` sind vorhanden, keine JS-Fehler.
- **`branding.html`** — Statisches Branding-Widget für die untere rechte Ecke („DaN | LIVE STREAM").
- **`overlay.html`** — **All-In-One Master-Szene** (1920×1080), kombiniert `goal.html` + `chat.html` + `avatar.html` + die Session-Stats/Rang-Logik aus `index.html` + `branding.html` auf **einer** Browser-Quelle, damit in Streamlabs Mobile nur ein einziger Link nötig ist. Die Einzelseiten bleiben unverändert als modulare Backup-Szenen bestehen — `overlay.html` ist eine Ergänzung, kein Ersatz. Layout: oben links Wanted-Level + Session-Stats/Rang gestapelt, oben rechts Follower-Goal, unten links Avatar, unten rechts Chat-Feed + Branding-Tag, unten mittig Soundtrack-Badge, Mitte Party-Popup/Alerts, vollflächig die FX-Layer. Technischer Vorteil der Zusammenlegung: alle drei `tmi.js`-Anwendungsfälle (Chat-Feed rendern, Chat-Commands, Avatar-Reaktion) laufen über **eine** gemeinsame Verbindung statt drei getrennter (die Einzelseiten brauchten getrennte Verbindungen, weil es getrennte Browser-Quellen waren). Session-Stats/Rang-Karte und Branding-Tag wurden dabei bewusst vom alten Glassmorphism-Look auf den "Urban Blackout"-Look umgestylt, damit die Master-Szene optisch aus einem Guss bleibt (die Original-Einzelseiten `index.html`/`branding.html` bleiben unverändert im alten Look). Chat-Stage ist auf eine feste Box unten rechts begrenzt (statt Vollbild wie in `chat.html`), damit sie nicht mit dem Avatar unten links kollidiert.
- **`start.html`** — Eigenständige **"Starting Soon"-Szene** (1920×1080, deckend schwarzer/radialer Hintergrund statt transparent — ersetzt das Gameplay kurz vor Stream-Start als eigene Streamlabs-Szene). **Runde 3 (aktuell): komplette Neufassung** nach einem zweiten, vom Nutzer in Claude Design gebauten Standalone-Export ("Twitch Starting Soon") — ersetzt die vorherige `CountdownPiece`-basierte Version vollständig. Teilt sich jetzt **ein gemeinsames visuelles System mit `in-game.html`** (parallel im selben Zug neu gebaut, aus einem zweiten, thematisch passenden Export desselben Nutzer-Design-Projekts): `Anton` (Display) + `Share Tech Mono` (Body) statt der vorherigen Chakra-Petch/JetBrains-Mono-Kombination, Blutrot `#ff2340` + wandernde Gold-Chrom-Lichtreflex-Animation (`chromeShine`-Keyframe) statt reinem Rot/Chrom. Export-Format diesmal ohne `<x-import>`/JSX-Dateien — ein neueres Bundler-Format mit `<script type="__bundler/template">` (JSON-String der fertigen HTML/CSS-Vorlage inkl. UUID-Platzhaltern für Bild-/Font-Assets) + `<script type="__bundler/manifest">` (UUID→base64-Bilddaten) — Extraktion per Python (`json.loads` + `base64.b64decode`) direkt aus den beiden Blobs, kein Ausführen des Bundlers nötig. Baer-Darstellung von einem einzelnen Low-Poly-Kopfbild mit CSS-Filter-Glitch auf **zwei echte Ganzkörper-Posen** (idle/action) umgestellt — aus dem Export extrahiert und per Bytevergleich bestätigt: die "Action"-Pose ist **byte-identisch** mit dem bereits vorhandenen `assets/images/avatar_action.png`, die "Idle"-Pose nahezu identisch mit `avatar_idle.png` — keine neuen Bild-Assets nötig, beide bestehenden Dateien direkt weiterverwendet. Wie beim vorherigen Import wurde jede feste px-Position/-Größe 1:1 nach `calc(var(--vu) * N)` übersetzt (kein `transform:scale()`). Countdown-/Ticker-Logik NICHT 1:1 aus dem Export übernommen: das Original loopt den Countdown per Default endlos (`loopCountdown:true`, gedacht für die Design-Tool-Vorschau) — hier bleibt die bereits bewährte Produktionslogik der Vorgänger-Version bestehen (schaltet bei 00:00 einmalig auf "JETZT LIVE" um, kein Endlos-Loop). Inhalt: Top-Leiste (Twitch-Handle + "SIGNAL: BEREITSCHAFT"), zentrale Spalte mit Tagline "◆ ALTE SCHULE HIP-HOP RADIO ◆", Bär (statische Pose, Default `avatar_action.png`), riesige Gold-Chrom-Headline ("GLEICH LIVE"/"JETZT LIVE" via `Anton`, 120px), gerahmter Countdown-Block (`?minutes=`-Parameter, Default 5) mit blinkendem Doppelpunkt, rotierender Status-Ticker (6 Meldungen, 1:1 aus dem Export), untere Leiste mit 20-Balken-EQ-Visualizer + Vinyl-Spinner + Track-Marquee (`?track=`/`?artist=`-Parameter). Vier statische HUD-Ecken-Klammern + Scanline-/Korn-Textur-Layer, konzentrische Ring-Dekoration hinter dem Bären. Getestet: Playwright bestätigt Bär-Bild lädt, 20 EQ-Balken rendern, Countdown schaltet bei 0:00 auf "JETZT LIVE" um (kein Loop-Hängenbleiben), keine echten JS-Fehler.
- **`in-game.html`** — **Zusätzliche** Gameplay-Szene (1920×1080, transparent, liegt wie `goal.html`/`chat.html`/`overlay.html` über dem Gameplay) — **kein Ersatz für `overlay.html`**, beide bleiben als getrennte, wählbare Streamlabs-Browser-Quellen-URLs bestehen. **Runde 2 (aktuell): komplette Neufassung** nach einem vom Nutzer in Claude Design gebauten Standalone-Export ("Twitch In-Game HUD") — ersetzt die erste, selbst entworfene "Vintage Hip-Hop/Gold-Bling"-Lederpalette-Version vollständig (die nicht dem tatsächlichen Nutzer-Design entsprach). Teilt sich jetzt dasselbe visuelle System wie das parallel neu gebaute `start.html` (siehe dort für Extraktions-Details/Bär-Bild-Herkunft: `Anton`+`Share Tech Mono`, Blutrot+Gold-Chrom, `chromeShine`-Animation, byte-identische Wiederverwendung von `assets/images/avatar_idle.png`/`avatar_action.png`). Nutzt weiterhin das `--vu`-Fluid-Unit-System (kein `transform:scale()`). Layout, alle Zonen `position:absolute` unter `#shake-root { position:fixed; inset:0; }`: **Bär oben links** (ersetzt die Webcam) — jetzt ein echter Bild-Crossfade (`.bear-layer-idle`/`.bear-layer-action`, 1:1 `triggerBearReaction()`-Timing aus `avatar.html`/`overlay.html`: 3.5s Anzeigedauer, 350ms Cooldown, Verlängerung bei Folge-Events) statt der vorherigen Low-Poly-Kopf-Glow-Simulation — ausgelöst durch Chat-Nachrichten/Twitch-Events (primär) sowie Mikrofon-Pegel (Bonus-Pfad, `tryStartMic()`, in Streamlabs Mobile bestätigt blockiert). **Event-Karten oben rechts** ("Letzter Follower"/"Letzte Subsprüche"/"Letzte Spende") — im Gegensatz zum Original-Design (das mit festen Demo-Werten arbeitet) zeigen sie echte, live aus der EventSub-Verbindung gespeiste "letztes Event"-Daten, vor dem ersten echten Event ein dezenter "Warte auf..."-Platzhalter statt erfundener Namen; **"Letzte Spende" bleibt bewusst an Bits/Cheers gekoppelt** (keine echte Spendenplattform im Projekt), zeigt aber jetzt echte Cheer-Nachrichten aus dem Event. **Chat "CHAT VIBES" rechts** (TV/Boombox-Rahmen-Optik mit Schrauben-Ecken, 1:1 aus dem Design) — `tmi.js` + XSS-sicheres Emote-Rendering, zeigt "WARTE AUF NACHRICHTEN" bis zur ersten echten Nachricht statt der zwölf Demo-Chat-Zeilen aus dem Export. **Hype-Level unten links** — 1:1 aus dem Export übernommene Logik: neuer, **bewusst eigenständiger** `localStorage`-Key `dangsxr_hypeLevel` (1-5, NICHT vereinheitlicht mit `goal.html`s `dangsxr_wanted_points` — explizite Nutzer-Entscheidung), klickbare Sterne + Cross-Tab-Sync per `storage`-Event aus dem Original übernommen, zusätzlich um echten Auto-Increment bei Sub/Cheer-Events ergänzt (im Original nur klickbar). Bei 5/5: pulsierender Rahmen (`hypePulse`, aus dem Design), kein Toast/Popup. **Bottom-Leiste**: 16-Balken-EQ + Vinyl-Spinner + Track-Marquee (`?track=`/`?artist=`) + MIC/CAM/NET-Status-Punkte (NET real an den EventSub-Verbindungsstatus gekoppelt, MIC/CAM dekorativ) + LIVE-Uptime-Zähler (zählt seit Laden der Seite). **Bewusst nicht Teil dieser Runde** (Zeitdruck vor dem ersten echten Stream-Einsatz): kein Rocket-League-Stats/Rang-Widget (nicht Teil des Original-Designs, geplante Ergänzung — siehe Offene Punkte), kein Live-Spotify-Ticker (Nutzer hat Spotify per MCP verbunden und sich für eine echte Live-Anzeige entschieden, braucht aber erst eine Spotify-Developer-App-Registrierung + OAuth-Token seinerseits, siehe Offene Punkte). `?test=follow|sub|resub|bits|react|hype|speak` fürs manuelle Durchtesten. Getestet: Playwright bestätigt beide Bär-Bilder laden, 16 EQ-Balken rendern, `?test=`-Pfade lösen die erwarteten Effekte aus (Event-Karten, Hype-Level-Increment, Bär-Reaktion, Max-Pulse), Sterne-Klick setzt `dangsxr_hypeLevel` korrekt, Chat-Rendering inkl. XSS-Escaping, keine echten JS-Fehler. **Nicht getestet, braucht Live-Validierung in Streamlabs Mobile**: RL-HUD-Kollisionsfreiheit der neuen Zonen, echtes Mikrofon-Verhalten, tatsächliche interne iframe-Auflösung.
- **`api/stats.js`** — Vercel Serverless Function. Fragt die Rocket League Tracker API (`api.tracker.gg`) für den Spieler **„DaNgsxR"** (Plattform: `epic`) ab und liefert `wins`/`matches` als JSON zurück, dazu ein `rank`-Objekt (`playlist`, `tierName`, `tierIconUrl`, `mmr`) aus demselben Response — kein zusätzlicher externer Call. Bevorzugt die Playlist `ranked-doubles`, fällt auf `ranked-standard` zurück, danach auf die erste Playlist mit gültigen Tier-Daten; liefert `rank: null`, wenn nichts Verwertbares gefunden wird (z.B. noch unranked in der Season). Cached das Ergebnis 30 Sekunden lang In-Memory (modulweite Variable, gilt pro warmer Function-Instanz) und liefert bei einem fehlgeschlagenen Tracker.gg-Request den letzten bekannten Stand als `stale: true` zurück, statt das Overlay einfrieren zu lassen. **Fehlerkorrektur: konkrete Diagnose statt generischer Meldung (aktuell)**: Nutzer öffnete `/api/stats` direkt im Browser (wie gebeten) und bekam `{"success":false,"message":"Tracker lädt noch..."}` — bestätigt, dass die eigene Vercel-Function laeuft, aber der Aufruf an `api.tracker.gg` fehlschlägt, ohne dass die alte Meldung erkennen liess WARUM (403 = blockiert/Rate-Limit, 404 = Spieler/Plattform nicht gefunden, 5xx = tracker.gg-seitiges Problem?). Response liefert bei einem fehlgeschlagenen Tracker.gg-Call jetzt zusätzlich `trackerStatus` (echter HTTP-Statuscode) und `trackerBodySnippet` (erste 300 Zeichen der Tracker.gg-Antwort); bei einer OK-Antwort ohne verwertbares `overview`-Segment zusätzlich `foundSegmentTypes` (welche Segment-Typen tatsächlich in der Antwort steckten). Aus der Sandbox nicht live gegen die echte Tracker.gg-API testbar (Netzwerk-Policy blockt ausgehende Domains) — mit gemocktem `fetch()` funktional gegen alle drei Fehlerpfade (403 mit Body, OK ohne overview-Segment, Happy-Path) getestet. **Ursache gefunden (aktuell)**: Nutzer teilte die jetzt detaillierte Antwort — `trackerStatus: 403`, `trackerBodySnippet` beginnt mit `"You've Been Blocked - Tracker Network"`. Das ist keine Rate-Limit-Verzögerung, sondern eine gezielte Bot-/Datacenter-Sperre seitens Tracker.gg (Cloudflare-artiges Verhalten gegen Cloud-Server-IPs wie die von Vercel/AWS) — betrifft die Vercel-Function selbst, unabhängig vom Nutzer-Gerät. Fix-Versuch: Request-Header von nur `User-Agent` auf ein vollständigeres, browser-typisches Set erweitert (`Accept`, `Accept-Language`, `Referer: https://rocketleague.tracker.gg/`, `Origin: https://rocketleague.tracker.gg`), um weniger wie ein Scraping-Bot auszusehen. **Kein Erfolgsversprechen**: greift die Sperre auf reiner IP-Ebene (Vercels Server-IP-Range pauschal geblockt), hilft das vermutlich nicht dauerhaft — dann bräuchte es entweder eine offizielle Tracker.gg-API-Registrierung (tracker.gg/developers, API-Key-basiert, bislang nicht eingerichtet) oder es bleibt beim bereits vorhandenen Offline-Fallback-Panel als dauerhaftem Verhalten. Nutzer gebeten, `/api/stats` nach diesem Deploy erneut zu prüfen. **Endgültiger Fix (aktuell): Umstieg auf die offizielle Tracker-Network-API**: Header-Fix bestätigt wirkungslos (weiterhin `trackerStatus: 403`, "You've Been Blocked"). Nutzer hat sich auf tracker.gg/developers registriert und einen offiziellen API-Key besorgt (Vercel-Umgebungsvariable `TRACKER_API_KEY`, neu benötigt). `api/stats.js` ruft jetzt `https://public-api.tracker.gg/v2/rocket-league/standard/profile/{platform}/{playerId}` (statt der privaten `api.tracker.gg`-Adresse) mit dem Header `TRN-Api-Key: ${TRACKER_API_KEY}` auf — ein autorisierter API-Aufruf statt eines Scraping-Requests, dadurch nicht von der IP-basierten Bot-Sperre betroffen. Die alten browser-spoofenden Header (User-Agent/Referer/Origin) wurden entfernt, da für einen authentifizierten API-Client nicht mehr nötig. Neue Guard-Klausel liefert eine klare Fehlermeldung, falls `TRACKER_API_KEY` fehlt, statt einen Request ohne Auth zu versuchen. Die Segment-Parsing-Logik (`overview`/`playlist`-Segmente, `findRank()`) wurde unverändert übernommen, da Tracker Network laut eigener Dokumentation für alle unterstützten Spiele dasselbe Datenformat liefert — aus der Sandbox nicht live gegen den echten Key verifizierbar (Netzwerk-Policy blockt die Domain), nur mit gemocktem `fetch()` getestet (korrekte URL/Header, Happy-Path mit Rang-Daten, fehlender Key, 401-Fehlerpfad). Nutzer gebeten, `/api/stats` nach diesem Deploy zu prüfen, ob die Segment-Struktur der offiziellen API tatsächlich mit der bisherigen Parsing-Annahme übereinstimmt. **URL-Korrektur (aktuell)**: Nutzer bekam `trackerStatus: 401` ("Invalid authentication credentials") trotz korrekt gesetztem `TRACKER_API_KEY` — Ursache war nicht der Key, sondern die URL selbst. Nutzer fand die offizielle Tracker-Network-Dokumentation (`tracker.gg/developers/docs/data-ingest`) mit dem tatsächlichen Code-Beispiel: der korrekte Pfad ist `https://public-api.tracker.gg/api/v1/{titleSlug}/standard/profile/{platform}/{playerId}` — der vorherige Code nutzte fälschlich `/v2/` ohne das `/api/`-Präfix. URL entsprechend auf `/api/v1/rocket-league/standard/profile/...` korrigiert, Header (`TRN-Api-Key`) unverändert (war bereits richtig). Aus der Sandbox weiterhin nicht live testbar — mit gemocktem `fetch()` verifiziert, dass die korrigierte URL tatsächlich angefragt wird. Nutzer gebeten, `/api/stats` erneut zu prüfen. **Endgültiges Ergebnis (aktuell): Rocket League nicht ueber die offizielle Tracker-Network-API verfuegbar — als dauerhafte Einschraenkung akzeptiert.** URL-Korrektur brachte weiterhin `trackerStatus: 401` ("Invalid authentication credentials"). Nutzer pruefte daraufhin auf eigene Initiative drei verschiedene Seiten im Tracker.gg-Entwickler-Portal ("Erste Schritte", "Datenaufnahme", "Authentifizierung") — auf allen dreien listet die "Spiel-APIs"-Sidebar konsistent nur **Apex Legends** und **Die Division 2**, Rocket League taucht nirgends auf, und es gibt keine Nutzer-seitige Moeglichkeit, es der eigenen App hinzuzufuegen. Damit ist geklaert: Tracker Network bietet aktuell schlicht keine Rocket-League-API fuer Drittentwickler an (der Key selbst ist vermutlich gueltig, kann aber nicht fuer ein nicht angebotenes Produkt autorisiert werden) — kein Konfigurationsfehler unsererseits. Per `AskUserQuestion` entschieden: **Offline-Fallback-Panel bleibt dauerhafter Zustand**, kein weiterer Fix-Versuch. `rl-stats.html`/`rl-daily.html`/`in-game.html`s RL-Stats-Karte zeigen weiterhin den D5/DANGSXR1000-Platzhalter statt echter Zahlen — das ist ab jetzt erwartetes, akzeptiertes Verhalten. Einziger verbleibender (nicht verfolgter) Weg waere eine individuelle Anfrage an `business@tracker.network` fuer custom Datenzugriff, ohne Erfolgsgarantie oder Zeitrahmen.
- **`avatar.html`** — Audio-reaktiver Avatar (Modul 3). **Live-Test bestätigt**: `getUserMedia()` wird in der Streamlabs-Mobile-Android-WebView blockiert (`NotAllowedError`, kein Berechtigungsdialog erscheint überhaupt — im normalen Mobile-Chrome funktioniert es einwandfrei). Design-Konsequenz: eine dauerhafte, mikrofonunabhängige Animation ist der **primäre** Zustand, nicht mehr nur Fallback. **Bild statt CSS**: das ursprüngliche CSS-gezeichnete Sturmhauben-Design gefiel optisch nicht — ersetzt durch zwei vom Nutzer bereitgestellte Bilder (`assets/images/avatar_idle.png` + `assets/images/avatar_action.png`, echte RGBA-Transparenz, ein "DaN"-gebrandeter Gangster-Bär in zwei Posen: ruhig stehend vs. Faust hoch/hyped), als `<img>`-Layer eingebunden. Struktur: `.avatar-bob` (äußerer Wrapper, trägt das permanente CSS-Keyframe-Idle-Wippen) umschließt `.avatar-punch` (JS-getriebener Bounce-Impuls bei jedem Event, eigene Transform-Quelle getrennt von `.avatar-bob` — gleiches Trennprinzip wie bei `#party-popup`/`#party-panel`) umschließt zwei gestapelte `<img class="avatar-layer">`-Elemente (`.avatar-layer-idle`, `.avatar-layer-action`), die per Opacity-Crossfade (0.4s) übereinander überblendet werden statt hart den `src` zu tauschen. Zustands-Logik (`triggerReaction()`/`triggerAvatarReaction()`): bei jedem Event (Chat-Nachricht, Chat-Command, Mikro-Peak) wechselt der Bär für **ca. 3.5s** auf `avatar_action.png` inkl. einem kontinuierlich pulsierenden Neon-Rot-Glow (`avatarGlowPulse`-Keyframe, passend zum "Urban Blackout"-Akzent) hinter dem Bild, danach automatisch fließend zurück auf `avatar_idle.png`; weitere Events während der 3.5s verlängern die Anzeigedauer (Bär bleibt "hyped", solange der Chat aktiv ist), statt hart abzuschneiden. In `overlay.html` reagiert der Avatar zusätzlich auf **Alerts** (`triggerAvatarReaction()` wird auch aus `showPartyPopup()` heraus aufgerufen, nicht nur aus dem Chat-Handler) — in der Einzelseite `avatar.html` gibt es diesen Alert-Kanal naturgemäß nicht (keine EventSub-Anbindung dort), nur Chat-Reaktion + Mikro-Bonus. **Mikro-Bonus** bleibt kostenlos im Code (löst dieselbe Event-Logik aus), schlägt er fehl (Regelfall in Streamlabs Mobile), läuft Idle/Chat unverändert weiter. `?test=speak` simuliert weiterhin einen Mikro-Pegel zu Testzwecken. **Runde 2 (aktuell) — "voll funktionsfähiger" freistehender Bär**: Nutzer wollte den Bären als eigene, in Streamlabs frei per Transform-Panel positionierbare Quelle nutzen können (statt fest im CSS von `in-game.html` einprogrammiert), nachdem der dort fest positionierte Bär wiederholt mit unterschiedlichen Rocket-League-Bildschirmen kollidiert war (siehe `in-game.html`-Hotfix-Historie unten). Drei Änderungen: (a) `.avatar-position` füllt jetzt randlos die eigene Seite aus (`inset:0`, kein fester `bottom/left`-Offset mehr) — Streamlabs' eigenes Transform-Panel (W/H/Position) entscheidet dadurch allein über Endgröße/Platzierung, keine doppelte Positionslogik. (b) Glow-Optik von der alten separaten, nur während Events sichtbaren Kreis-Glow-Div (`--blood: #ff1a1a`) auf denselben permanenten, der Bär-Silhouette folgenden Drop-Shadow-Glow umgestellt, den `in-game.html` seit dessen eigener Kasten-Entfernung nutzt (`--blood: #ff2340`) — visuelle Parität mit dem dort bereits bestätigten Look. (c) Echte Twitch-EventSub-Anbindung ergänzt (Follow/Sub/Cheer/Raid → `triggerReaction()`, 1:1 Verbindungslogik aus `overlay.html`/`in-game.html`) — vorher reagierte diese Einzelseite mangels EventSub nur auf Chat, jetzt genauso wie der Bär in den Bündel-Seiten. Getestet: Playwright bestätigt `.avatar-position` füllt den Viewport exakt aus, beide Bilder laden, `?test=follow` löst den Action-Zustand aus, keine JS-Fehler. **Runde 3 (aktuell) — Tier-gestaffelte Reaktionen + Idle-Flex**: Nutzer wollte "mehr Bewegung, mehr Interaktion, mehr Stil" für den Bären. Da nur zwei Bilder existieren (keine neuen Posen verfügbar), komplett über CSS/JS statt neuer Grafik-Assets gelöst: `triggerReaction()` nimmt jetzt einen `tier`-Parameter (`light`=Chat/Mikro, `medium`=Follow, `strong`=Sub/Resub/Cheer, `epic`=Raid) plus optionalen Callout-Text. Jeder Tier hat eine eigene Punch-Stärke (vier `.avatar-punch.punch-*`-Klassen mit steigendem Ausschlag), eine eigene Glow-Farbe (`--blood` wird per JS temporär umgesetzt: Rot=light, Blau=medium/Follow, Gold=strong/Sub-Cheer, mit Reset auf den Ausgangswert nach der 3.5s-Aktionsdauer), einen Partikel-Burst (neues, bewusst simples `<canvas>`-System, deutlich schlanker als `goal.html`s `SpraySystem`, keine Partikel bei `light` um Chat-Spam nicht zu überladen) und einen kurzen Text-Callout (z.B. "NEUER FOLLOWER!"/"SUB!"/"RESUB!"/"CHEER!"/"RAID!", nur ab `medium` aufwärts). `epic` (Raid) bekommt zusätzlich einen kurzen Screen-Shake auf `.avatar-position` und einen Regenbogen-Farbwechsel-Puls auf den Bild-Layern (`avatarEpicGlow`-Keyframe, direkt auf `filter` animiert statt über die `--blood`-Variable, da animierte CSS-Custom-Properties für Farbwerte ohne `@property`-Registrierung nicht zuverlässig interpolieren). Zusätzlich eine Idle-"Flex"-Geste (`scheduleIdleFlex()`, alle 25-40s mit Zufalls-Jitter, größerer Bewegungsausschlag als die normale Dauerwippe) für mehr Lebendigkeit auch in den langen Phasen ohne Event. `?test=` um `raid` und `idle` erweitert. **Bug gefunden und gefixt**: `lastPunchAt` startete bei `0`, aber `performance.now()` liegt beim allerersten Reaktionsaufruf kurz nach Seitenladen oft noch unter der 350ms-Cooldown-Schwelle — dadurch wurde die allererste Reaktion nach jedem Neuladen der Quelle (Punch/Glow/Partikel/Callout) fälschlich komplett übersprungen; existierte schon vor dieser Runde (betraf bisher nur den Punch-Bounce), ist mit den neuen Glow-/Partikel-/Callout-Effekten aber spürbarer geworden. Fix: `lastPunchAt` startet jetzt bei `-Infinity`. **Bug gefunden und gefixt (Position)**: Callout initial über dem Bären (`bottom:62%`) platziert — bei enger Bild-Rahmung (Bär füllt fast die gesamte Containerhöhe) überlappte der Text mit Kopf/Cap statt frei zu schweben; Screenshot-Kontrolle zeigte spürbar mehr freien Raum unterhalb der Füße als oberhalb des Kopfes, Callout daher auf `bottom:2%` (unterhalb des Bären) umgestellt. Getestet: Playwright bestätigt alle vier Tiers lösen die jeweils korrekte Punch-Klasse/Glow-Farbe/Callout-Text aus, Raid löst zusätzlich Screen-Shake + Regenbogen-Puls aus, `light`-Tier (Chat/Mikro) zeigt bewusst keinen Callout, `?test=idle` löst die Flex-Geste sofort aus, keine JS-Fehler; Screenshot-Kontrolle bei Follow/Sub/Raid bestätigt lesbare, nicht überlappende Callouts. **Runde 4 (aktuell) — Chat-Commands für alle Zuschauer**: Nutzer wollte "mehr Spielereien" im Chat, konkret mehr Interaktionsmöglichkeiten für den Bären. Vier neue Chat-Commands ergänzt, bewusst **offen für jeden Zuschauer** (kein Mod-Check) — im Unterschied zu `goal.html`s Mod-only `!shoot`/`!siren`/`!tor`/`!fail`, die absichtlich moderiert bleiben (Missbrauchspotenzial bei Sirene/Screen-Effekten höher als bei einer Bären-Reaktion): `!tanz` (medium-Tier + eigenes Tanz-Wiggle, siehe unten), `!flex` (strong-Tier), `!hype` (strong-Tier), `!wave` (medium-Tier), jeweils mit eigenem Callout-Text ("TANZT!"/"FLEXT!"/"HYPE!"/"WINKT!"). Nutzt die bestehende `triggerReaction(tier, calloutText)`-Infrastruktur aus Runde 3 unverändert — kein neuer Reaktionsmechanismus, nur neue Aufrufer. Neuer, von `PUNCH_COOLDOWN_MS` (350ms) getrennter `COMMAND_COOLDOWN_MS` (2500ms): Commands lösen Partikel/Callout/teils Tanz-Wiggle aus, das ist deutlich "lauter" als die dezente Light-Reaktion auf jede normale Chat-Nachricht und soll sich nicht spammen lassen (gleiches Cooldown-Prinzip wie `goal.html`s `!shoot`). `!tanz` bekommt zusätzlich eine eigene, rhythmischere CSS-Bewegung (`@keyframes avatarDance`, `.avatar-bob.dancing`, 1.6s einmalig) statt nur des generischen Punch-Bounce, damit sich der Befehl auch spürbar anders anfühlt als `!flex`/`!hype`/`!wave` trotz teils gleichem Tier. Der bestehende Chat-Handler (`reactChatClient.on('message', ...)`) prüft jede eingehende Nachricht zuerst gegen die neue `CHAT_COMMANDS`-Map (case-insensitiv, getrimmt) und fällt nur bei Nicht-Treffer auf die bisherige unconditional `light`-Reaktion zurück — normale Chat-Nachrichten verhalten sich dadurch unverändert. `?test=` um `tanz`/`flex`/`hype`/`wave` erweitert (ruft `handleChatCommand()` direkt mit dem jeweiligen Befehlstext auf, gleiches Prinzip wie die bestehenden EventSub-Test-Pfade). Getestet: Playwright bestätigt alle vier Commands lösen den richtigen Tier (Glow-Farbe)/Callout-Text aus, `!tanz` aktiviert zusätzlich die `dancing`-Klasse, `!flex`/`!hype`/`!wave` NICHT; ein simulierter echter Chat-Nachrichten-Fluss (gemockter `tmi.Client`) bestätigt case-insensitive/getrimmtes Command-Matching sowie dass ein sofort nachfolgender zweiter Command durch `COMMAND_COOLDOWN_MS` unterdrückt wird; eine normale (Nicht-Command-)Chat-Nachricht löst weiterhin nur die dezente `light`-Reaktion ohne Callout aus; bestehende Tier-Regressionstests aus Runde 3 laufen unverändert grün; keine JS-Fehler.
- **`rl-stats.html` / `status-ticker.html` / `event-cards.html` / `chat-vibes.html` / `hype-level.html` / `status-bar.html`** — sechs neue, frei positionierbare Einzel-Widget-Quellen, aus `in-game.html` extrahiert (gleicher Anlass wie bei `avatar.html`s Runde 2 oben: Nutzer wollte JEDES Widget einzeln per Streamlabs-Transform-Panel positionieren können, nachdem das fest positionierte `in-game.html`-Bündel wiederholt mit unterschiedlichen Rocket-League-Bildschirmen kollidierte — siehe `in-game.html`-Hotfix-Historie unten für die volle Vorgeschichte). Inhaltlich/funktional 1:1 identisch zu den entsprechenden Widgets in `in-game.html` (gleiche CSS-Klassen, gleiche Chrome/Gold-Bling-Optik), nur aus dem Bündel herausgelöst: `rl-stats.html` (RL-Session-Stats-Karte, eigenes Polling gegen `api/stats.js`), `status-ticker.html` (rotierende `?ticker=`-Meldungen), `event-cards.html` (Follower/Sub/Bits-Kartenstapel, eigene EventSub-Verbindung), `chat-vibes.html` (Chat-Panel im TV/Boombox-Look, eigene tmi.js-Verbindung — bewusst NICHT dasselbe wie `chat.html`, das den anderen "Urban Blackout"-Fall-Physik-Stil nutzt), `hype-level.html` (5-Sterne-Meter, eigene EventSub-Verbindung für Auto-Increment, Key weiterhin bewusst `dangsxr_hypeLevel`), `status-bar.html` (EQ+Vinyl+Track-Marquee+MIC/CAM/NET+Uptime, NET-Punkt an eigene EventSub-Verbindung gekoppelt). Alle sechs füllen randlos die eigene Seite aus (kein `--vu`, da als kleine Einzel-Quelle gedacht — `--vu: 1px` als fixe Konstante, damit die vorhandenen `calc(var(--vu) * N)`-Werte unveraendert 1:1 aus `in-game.html` uebernommen werden konnten) — siehe `STREAMLABS_SETUP.md` für die Erklärung der zwei Quellen-Arten (vollflächige Master-Szenen vs. frei positionierbare Einzel-Widgets). Bewusster Kompromiss: mehrere gleichzeitig genutzte Einzel-Quellen bedeuten mehrere parallele EventSub-/tmi.js-Verbindungen statt einer gebündelten (wie es umgekehrt gerade der Vorteil von `overlay.html`/`in-game.html` war) — Twitch erlaubt das problemlos, es ist lediglich weniger effizient; wer das nicht braucht, nutzt weiterhin das `in-game.html`-Bündel. `in-game.html` selbst bleibt dabei unverändert bestehen (Ergänzung, kein Ersatz, gleiches Prinzip wie schon bei `overlay.html` vs. den ursprünglichen Einzelseiten). Getestet: Playwright bestätigt für alle sechs Seiten korrektes Rendering, funktionierende `?test=`-Parameter, klickbare Hype-Sterne, XSS-sicheres Chat-Escaping, keine JS-Fehler. **Runde 2 (aktuell)**: Nutzer-Screenshot nach dem Einrichten in Streamlabs zeigte mehrere Widgets (Hype-Level, Chat Vibes, Ticker) winzig und abgeschnitten statt wie vorgesehen. Ursache: `--vu` war in diesen sechs Dateien fest auf `1px` gesetzt, in der Annahme, Streamlabs' Transform-Panel würde den Inhalt beim Verkleinern proportional mitskalieren — tut es aber nicht, es ändert nur die Render-Viewport-Größe der Quelle, alles was bei `overflow:hidden` nicht mehr reinpasst wird schlicht abgeschnitten statt verkleinert. Fix: `--vu` in allen sechs Dateien von der festen `1px`-Konstante auf eine `vw`-basierte `clamp()`-Formel umgestellt, jeweils auf die natürliche Design-Breite der jeweiligen Karte kalibriert (z. B. `clamp(0.3px, 0.263vw, 3px)` für die ~380px breite RL-Stats-/Hype-Karte) statt auf die 1920px-Referenzbreite der vollflächigen Szenen — exakt dasselbe Grundprinzip wie das etablierte `--vu`-System, nur pro Widget auf dessen eigene Design-Größe bezogen. `avatar.html` war von diesem Bug nicht betroffen (nutzt bereits `width/height:100%` + `object-fit:contain` ohne `--vu`). Getestet: Playwright bestätigt für alle sechs Dateien bei der Hälfte ihrer Design-Größe, dass der Inhalt vollständig sichtbar bleibt (nicht mehr abgeschnitten), Screenshot-Kontrolle bei Hype-Level/Chat-Vibes bestätigt lesbaren, proportional verkleinerten Inhalt. **Runde 3 (aktuell)**: Nutzer meldete, `chat-vibes.html` zeige seinen echten Twitch-Chat nicht an. Ursache aus der Sandbox nicht abschließend diagnostizierbar (kein Zugriff auf `cdn.jsdelivr.net`/Twitch-IRC von hier aus), plausibelster Verdacht: `tmi.js` wird von der Streamlabs-Mobile-WebView aus nicht zuverlässig vom CDN geladen (im Gegensatz zur EventSub-Verbindung, die ohne externe Library als natives WebSocket funktioniert und beim Nutzer nachweislich verbindet). Statt weiter zu raten: `chat-vibes.html` bekommt jetzt einen sichtbaren Verbindungsstatus ("Verbinde…"/"Verbunden"/"Fehler", ersetzt die vorherige simulierte "Zuschauerzahl", die ohnehin nur die Anzahl gerenderter Nachrichten war, keine echte Twitch-Zahl) — inkl. explizitem Check, ob `tmi.js` überhaupt vom CDN geladen wurde, statt eines stillen `console.warn`. Damit kann der Nutzer beim nächsten Live-Test selbst sehen, ob die Verbindung überhaupt zustande kommt. Getestet: Playwright bestätigt Status wechselt zu "Fehler" bei blockiertem CDN-Script (simuliert), Ausgangszustand ist "Verbinde…". **Runde 4 (aktuell)**: Nutzer-Screenshot bestätigte live "Fehler" — die Diagnose funktioniert also. Status jetzt weiter aufgeschlüsselt in **"CDN-Fehler"** (`tmi.js` selbst kam nie vom CDN an, `typeof tmi === 'undefined'`) vs. **"Verbindungsfehler"** (`tmi.js` ist da, aber die IRC-WebSocket-Verbindung zu Twitch scheitert oder trennt) — beide Ursachen brauchen komplett unterschiedliche Fixes (CDN wechseln/selbst hosten vs. etwas anderes), ein generisches "Fehler" reichte nicht zum Eingrenzen. Getestet: Playwright bestätigt "CDN-Fehler" bei blockiertem Script.
- **`rl-daily.html`** (neu) — Schwester-Widget zu `rl-stats.html`, gleicher Chrome/Gold-Bling-Look/Kartenaufbau, aber anderes Reset-Verhalten: `rl-stats.html` zeigt eine **Session** (setzt bei jedem Neuladen der Browser-Quelle auf 0 zurück), `rl-daily.html` zeigt einen **Tag** (Nutzer-Wunsch: "die Anzeige bleibt immer für einen Tag, um 00:00 Uhr wird der Zähler automatisch auf Null gestellt") — bleibt über Neuladen/Streamlabs-Neustart hinweg bestehen und setzt sich nur beim Datumswechsel zurück. Wiederverwendet dieselbe `api/stats.js` (`wins`/`matches`, kein neuer Server-Code) — der einzige Unterschied zu `rl-stats.html` ist die Herkunft der Baseline: dort ein In-Memory-Wert vom Seitenaufruf, hier ein in `localStorage` (Key `dangsxr_rl_daily_baseline`) gespeichertes `{date, wins, matches}`-Objekt, das nur erneuert wird, wenn das gespeicherte Datum vom heutigen abweicht. Da Streamlabs-Browser-Quellen typischerweise stundenlang durchlaufen (kein Reload um Mitternacht), prüft der ohnehin laufende 20s-Poll bei jedem Tick, ob sich das Datum geändert hat, statt sich auf einen Seiten-Reload zu verlassen — der Reset passiert dadurch spätestens 20s nach Mitternacht, ganz ohne eigenen Timer. Getestet: Playwright bestätigt Erstaufruf zeigt sofort 0/0 (Baseline wird beim ersten Poll gesetzt), ein Reload am selben Tag zeigt weiterhin die korrekte Differenz zur gespeicherten Baseline (kein Reset), eine Baseline mit einem alten Datum (simuliert "gestern") wird beim nächsten Poll automatisch verworfen und auf 0/0 zurückgesetzt, Offline-Fallback-Panel schaltet sich bei einem API-Fehler korrekt frei.
- **`api/twitch-eventsub.js`** — Vercel Serverless Function. Holt per Refresh-Token einen frischen Twitch-User-Access-Token, ermittelt die Broadcaster-ID für den Kanal **„dangsxr1000"** und meldet für eine übergebene WebSocket-Session-ID die EventSub-Subscriptions `channel.follow`, `channel.subscribe`, `channel.subscription.message`, `channel.cheer` und `channel.raid` bei der Twitch-Helix-API an. Liest `TWITCH_CLIENT_ID`, `TWITCH_CLIENT_SECRET` und `TWITCH_REFRESH_TOKEN` aus den Vercel-Umgebungsvariablen.
- **`api/twitch-followers.js`** — Vercel Serverless Function (neu, siehe „Fehlerkorrektur: echte Follower-Zahl" unten). Holt per Refresh-Token (dieselben drei Env-Variablen, kein neuer Scope nötig) die **echte, aktuelle Gesamt-Follower-Zahl** von Twitch (Helix `GET /channels/followers`, `total`-Feld) für den Kanal „dangsxr1000", zusätzlich Name/Zeitpunkt des **zuletzt gefolgten Users** (`latestFollower`, erstes Element von `data` — Twitch liefert hier den jüngsten Follow zuerst). 60s In-Memory-Cache nach demselben Muster wie `api/stats.js`. Wird von `goal.html`/`overlay.html` beim Laden einmalig abgefragt, um den bis dahin hart einprogrammierten Follower-Platzhalter durch den echten Wert zu ersetzen; `event-cards.html`/`in-game.html` nutzen `latestFollower`, um die „Letzter Follower"-Karte gleich beim Laden mit dem echten letzten Follower zu füllen statt nur auf ein neues Live-Event zu warten. Für Subs/Cheers gibt es kein vergleichbares Helix-Endpoint — die „Letzte Subsprüche"/„Letzte Spende"-Karten bleiben bewusst rein Live-Event-basiert, das ist eine echte Twitch-API-Grenze, keine Auslassung.

## Deployment & Hosting

- Hosting über **Vercel**.
- **Auto-Deploy**: Jeder Commit auf den `main`-Branch löst automatisch ein neues Deployment aus.
- `api/stats.js` läuft als Vercel Serverless Function (Node-Runtime), die HTML-Dateien werden statisch ausgeliefert.

## Styling-Guides

**Zwei getrennte visuelle Systeme im Repo — nicht vermischen:**

### `goal.html` + `chat.html` + die Hood-Widgets in `overlay.html` — "Urban Blackout"-Look (aktuell, Stand 28.07.2026 — löst den frühere "Hood/Ghetto"-Look ab)
Kurskorrektur-Rebrand: der alte braune, zerrissene "Schrottplatz/Western"-Look (Rost-Töne, 14-Punkt-Papierriss-`clip-path`) wurde durchgehend gegen ein tiefschwarzes, hartkantiges Street-/Tech-Panel-Design ersetzt, weil er nicht als "Hood/Gangster" gelesen wurde. Betroffen sind alle Karten/Panels in `goal.html`, `chat.html` und den entsprechenden Widgets in `overlay.html` (`.hud-card`, `#party-panel`, `.chat-card`, `.soundtrack-badge`, `.branding-tag`) — das komplette Alert-/Audio-/Physik-System (Hydraulik, Spray, Sirene, Muzzle-Flash) blieb funktional unverändert, nur die Chrome-Optik wurde ausgetauscht.
- **Grundfarben**: tiefes Schwarz/Anthrazit (`--asphalt: #08090a`, `--asphalt-mid: rgba(9,9,11,0.92)`, `--steel: #202226`) statt Asphalt/Rost — "getönte Autoscheiben"-Optik über halbtransparentes Schwarz + `backdrop-filter: blur()`. Chrome-Töne (`--chrome-1: #eef0f2`, `--chrome-2: #8b8d93`) leicht kühler/weißer als zuvor.
- **Zwei Akzentfarben, sparsam eingesetzt**: aggressives Neon-Rot `--blood: #ff1a1a` (Rahmen, Glow, Sirene/Impact-Flash — heller/knalliger als das alte gedeckte Blutrot) und Gold `--gold: #ffd700` (Titel, Police-Tape, Badges).
- **Typografie**: unverändert `Bebas Neue` (condensed, Versalien) für Überschriften/Namen/Labels, `Montserrat` für Fließtext/Zahlen — klare Lesbarkeit hat weiterhin Priorität vor Verzerrungseffekten.
- **Harte, cleane Kanten statt zerrissenem Papier**: die alte 14-Punkt-"Panzerplatte"-`clip-path`-Polygon wurde durch eine einfache gekappte Tech-Panel-Ecke ersetzt (`clip-path: polygon(var(--cut) 0, 100% 0, 100% calc(100% - var(--cut)), calc(100% - var(--cut)) 100%, 0 100%, 0 var(--cut))`, ein CSS-Var `--cut` steuert die Kappungsgröße einheitlich) — zwei gegenüberliegende Ecken schräg gekappt, alle anderen Kanten scharf/gerade. `.shimmer-frame` (der Karten-Rahmen) ist von einem bunt wandernden Gold/Rot/Chrome-Farbverlauf auf einen schlichten, leise pulsierenden Rot/Stahl-Rahmen (`framePulse`-Keyframe, nur `filter: brightness()`) reduziert.
- **Chainlink-Fence-Textur statt Korn-Raster**: `::before`-Pseudo-Elemente auf allen Karten zeichnen ein feines Maschendraht-Muster aus zwei gegenläufigen `repeating-linear-gradient`-Diagonalen (55°/-55°, sehr niedrige Deckkraft) statt des alten Punktraster-Korns.
- **Police-Tape-Akzente in Gelb/Schwarz**: die bestehenden vollflächigen `.crime-tape`-Streifen im Party-Popup blieben (passten bereits zum Motiv), zusätzlich neu: ein kleiner `.tape-corner`-Eckstreifen (gelb/schwarz diagonal) sitzt auf jeder HUD-Karte (`goal-card`, `wanted-card`, `stats-card`) direkt über der gekappten Ecke, wie ein Flicken auf einem beschädigten Panel.
- **Branding im Rahmen**: dezente `.hud-watermark`-Beschriftung ("DANGSXR1000"/"DAN", niedrige Deckkraft, Bebas Neue, unten rechts in jeder Karte) statt eines separaten Branding-Elements — direkt ins Panel integriert.
- **Beschädigungs-Motive bleiben als Ornament**: statische Einschusslöcher (`.bullet-hole`, jetzt schwarz statt braun-schwarz), Riss-Overlay (SVG), "MOST WANTED"-Patch und Nummernschild-Badge — unverändert, da bereits schwarz/weiß/rot/gold und somit stilistisch kompatibel mit dem neuen Look.
- **Audio-Reaktivität als Kernprinzip**: unverändert — fast jeder visuelle Effekt (Hydraulik-Hop, Liquid-Bar-Wellen, Spray-Partikel-Rate, Sirene) hängt am `AudioAnalysisRig`-Sub-/Mid-Band-Signal, nicht an festen Timern. Die JS-seitigen Partikel-/Alert-Farbkonstanten (Spray-Canvas, Liquid-Bar-Gradient, `ALERT_CONFIG`) wurden bei dieser Runde bewusst **nicht** angetastet (weiterhin `#ffd700`/`#e8e6e1`/`#b30000`-Familie) — Scope war explizit die CSS-Chrome der Karten/Panels, nicht das bereits ausgetestete FX-System.

### `index.html` + `branding.html` — ursprünglicher Glassmorphism-Look (unverändert seit dem ersten Redesign)
- **Dark Mode** als Basis: Hintergrundflächen in Slate-900-Ton (`rgba(15, 23, 42, ...)`).
- **Neon-Akzente**: Blau `#38bdf8` und Orange `#f97316` als primäre Akzentfarben; ergänzend Grün `#22c55e`, Rot `#ef4444` und Gelb/Amber `#fbbf24` für Statuswerte.
- **Abgerundete Ecken**: `border-radius` durchgängig zwischen 10–20px.
- **Blur-Effekte**: `backdrop-filter: blur(8px)` mit halbtransparenten Hintergründen (`rgba(15, 23, 42, 0.9–0.96)`).
- Bewusst **nicht** an den Urban-Blackout-Look angeglichen — offener Punkt, siehe "Aktueller Stand & Offene Punkte".

## Hinweis (Sicherheit & Setup)

`goal.html` bezieht Follow-/Sub-/Bits-/Raid-Events seit der Migration auf Twitch EventSub **nicht mehr über Streamlabs** — `api/streamlabs-proxy.js` wurde entfernt, da nichts mehr den Streamlabs-Socket-Token benötigt. Die Umgebungsvariable `STREAMLABS_SOCKET_TOKEN` kann in den Vercel-Projekteinstellungen gelöscht werden. Unabhängig davon bleibt die frühere Empfehlung gültig: Der alte Token stand einmal im Klartext im Repo und in der Git-Historie und sollte im Streamlabs-Dashboard rotiert/widerrufen werden, falls das noch nicht geschehen ist.

**Neu benötigt für `api/twitch-eventsub.js`** — folgende Vercel-Umgebungsvariablen müssen gesetzt sein, sonst liefert die Function einen Fehler und `goal.html` kann keine Events abonnieren:

- `TWITCH_CLIENT_ID` / `TWITCH_CLIENT_SECRET` — aus einer selbst registrierten App auf [dev.twitch.tv/console](https://dev.twitch.tv/console).
- `TWITCH_REFRESH_TOKEN` — aus einem einmalig manuell durchgeführten OAuth-Authorization-Code-Flow des Broadcaster-Accounts (DaN) mit den Scopes `moderator:read:followers`, `channel:read:subscriptions`, `bits:read`. Der Access-Token selbst wird bei jedem `goal.html`-Laden frisch aus diesem Refresh-Token erzeugt, muss also nicht manuell erneuert werden — nur der Refresh-Token muss einmalig beschafft und als Env-Variable hinterlegt werden.

**Streamlabs-Mobile-Browser-Quellen-Setup**: Für jede neue Browser-Quelle (egal welche Szene) gilt ein festes, recherchiertes + live-bestätigtes Einrichtungs-Preset (Transform-Werte W/H/Sc, Shutdown/Refresh-Optionen, Begründung + Troubleshooting-Tabelle) — siehe **`STREAMLABS_SETUP.md`**. Entstanden, damit die richtigen Einstellungen nicht bei jeder neuen Szene erneut per Trial-and-Error über Live-Screenshots gefunden werden müssen.

## System & Hardware Context

- **Setup-Typ**: 100% Cloud- & Mobile-Streaming Setup (kein PC/Laptop).
- **Gaming-Plattform**: GeForce NOW Ultimate (Rocket League).
- **Hauptgerät (Gaming & Stream-Regie)**: Samsung Galaxy Tab S11 Ultra (Streamlabs Mobile).
- **Begleitgerät (Chat, Control & Twitch-App)**: Samsung Galaxy S26 Ultra.
- **Entwicklungs-Umgebung**: Claude Code Cloud CLI (Web Browser).
- **Canvas (Design-Referenz)**: alle Overlays werden konzeptionell für 1920×1080 (16:9) entworfen und per `--vu`-Fluid-Unit-System darauf kalibriert — die tatsächliche Streamlabs-Mobile-Projekt-Canvas des Tab S11 Ultra ist davon abweichend (16:10, siehe `STREAMLABS_SETUP.md` für den zuletzt bestätigten realen Wert), was dank `--vu` keine Code-Anpassung erfordert, aber bei jeder neuen Browser-Quelle im Transform-Panel korrekt eingetragen werden muss.
- **Streaming-Plattform**: Twitch.
- **Twitch Kanal**: DaNgsxr1000.
- **Ingame ID**: DaNgsxR (Epic Games).

## Twitch-Profil (Kanalseite, außerhalb des Overlay-Codes)

Zusätzlich zu den Overlays wurde die eigentliche Twitch-Kanalseite (twitch.tv/dangsxr1000) analysiert und neu konzipiert — zwei Dateien im Repo-Root dokumentieren das:

- **`twitch_profile_dump.json`** — Schnappschuss des Kanal-Zustands (Bio, Panels, Goals-Widget, Tags, Emotes, Monetarisierungsstatus, Erweiterungen). Automatisierter Live-Abruf über `decapi.me`/`twitch.tv` ist aus der Sandbox-Umgebung netzwerkseitig blockiert (HTTP 403 auf Proxy-Ebene, mit `curl` und `WebFetch` bestätigt) — die Daten stammen stattdessen aus vom Nutzer bereitgestellten Screenshots, transparent als `manual_screenshot_review` gekennzeichnet.
- **`PANEL_CONCEPT.md`** — vollständiges Redesign-Konzept: neue 5-Panel-Architektur (Setup, Kiez-Regeln, Über Mich/Die Hood, Socials, Support) mit exakten, copy-paste-fertigen Texten im Hood-Tone-of-Voice, korrigierte Kanal-Beschreibung (behebt kaputtes Markdown im Original), korrigierter Live-Benachrichtigungstext, einheitliche Bild-Spezifikation für alle 5 Panel-Grafiken (320×320px, Asphalt/Blutrot/Gold, Panzerplatten-Ecke, Einschusslöcher).
- **Wichtigster inhaltlicher Fund**: die alten Panel-Texte ("Mein Setup", "Über mich") nannten PS4 + DualShock 4 — das widerspricht dem oben dokumentierten tatsächlichen Setup (GeForce NOW Ultimate, 100% Cloud/Mobile). In den neuen Texten in `PANEL_CONCEPT.md` bereits korrigiert.

## Aktueller Stand & Offene Punkte (Stand 28.07.2026)

**Fertig & gemerged:**
- Vollständiges Hood/Ghetto-Redesign von `goal.html`/`chat.html` inkl. Audio-Engine, Hydraulik-Physik, Muzzle-Flash/Cop-Siren, Idle-Ambient-Layer.
- Migration von Streamlabs-Socket auf direkte Twitch-EventSub-Anbindung.
- `chat.html`-Routing-Fix (`?channel=`-Parameter + Demo-Modus, kein Redirect mehr).
- Twitch-Profil-Analyse + Panel-Redesign-Konzept (`twitch_profile_dump.json`, `PANEL_CONCEPT.md`).
- Strategie-Roadmap als Artifact veröffentlicht: **https://claude.ai/code/artifact/1d8195c9-76a1-4c89-9de7-1f57c5001c49** (6 Phasen: Overlay-Fundament → Profil-Grundgerüst → Engagement ohne Cam&Mic → Community&Reichweite → Monetarisierung → Pflege).
- **Modul 1 (Wanted-Level + Chat-Commands)**: GTA-Wanted-Level-Widget (5 Sterne, Sub/Bits-Fortschritt) + Chat-Commands `!shoot`/`!siren`/`!tor`/`!fail` in `goal.html`. Ersetzt den technisch nicht machbaren automatischen RL-Tor-Detektor durch manuelle Mod-Trigger. **Kurskorrektur**: ursprünglich mit Vercel-KV-Backend gebaut (PR #20), auf expliziten Wunsch noch am selben Tag wieder auf reines `localStorage` zurückgebaut (PR #21) — bewusste Team-Entscheidung gegen jede externe Datenbank, siehe Tech-Stack-Entscheidung unten. Kein Vercel-KV, kein `GOAL_API_SECRET` mehr nötig.
- **Modul 2 (RL Rang/MMR-HUD)**: `api/stats.js` liefert jetzt zusätzlich Rang-Tier + MMR (aus derselben Tracker.gg-Antwort, kein neuer Call), `index.html` zeigt Icon/Tier/MMR im bestehenden Session-Stats-Widget an, sobald Daten vorhanden sind.
- **Modul 4 (Soundtrack-Badge)**: animierte Laufschrift unten mittig in `goal.html`, Text aus `?track=`-URL-Parameter, kein Server/Speicher nötig.
- **Modul 3 (Avatar)**: `avatar.html` — Live-Test bestätigte, dass Mikro-Zugriff in der Streamlabs-Mobile-WebView blockiert ist. **Fertig inkl. Bild-Swap-Animation**: CSS-Masken-Design komplett entfernt, stattdessen zwei echte RGBA-Bilder (`assets/images/avatar_idle.png` + `avatar_action.png`, nach zwei gescheiterten Upload-Versuchen — Details siehe Punkt 13 unten — jetzt korrekt transparent). Permanentes CSS-Idle-Wippen als Dauerzustand; bei jedem Event (Chat-Nachricht, Command, in `overlay.html` zusätzlich Alerts) wechselt der Bär per Opacity-Crossfade für ca. 3.5s auf die Action-Pose inkl. pulsierendem Neon-Rot-Glow, danach fließend zurück (Kernprinzip „Chat ersetzt die Stimme"). Mikro-Reaktivität bleibt als kostenloser Bonus-Pfad im Code. `overlay.html` entsprechend mitgezogen (inkl. der zusätzlichen Alert-Anbindung, die die Einzelseite mangels EventSub dort nicht hat). Playwright-getestet: Bild-Swap-Timing, automatischer Reset, gleichzeitige Reaktion auf Chat- und Alert-Events, visuell per Screenshot (Idle- und Action-Pose) verifiziert.
- **Hotfix-Historie: `overlay.html`-Layout, vier Runden.** **Runde 1 (verworfen)**: `html, body` war fest auf `width:1920px; height:1080px` gesetzt, aber `#shake-root` (Eltern-Wrapper aller Widgets) nutzte `position:fixed; inset:0` — das positioniert sich per CSS-Spec immer relativ zum tatsächlichen Viewport, nicht zur deklarierten `body`-Größe. Erster Fix-Versuch: ein `#stage-scale`-Wrapper (fest 1920×1080), der per JS (`fitStage()`, `window.innerWidth/innerHeight`) gleichmäßig auf die reale Fenstergröße skaliert/zentriert wurde (Letterbox/Pillarbox). **Funktionierte im Playwright-/Desktop-Test, aber laut Nutzer-Live-Test NICHT in Streamlabs Mobile auf Android** — die App rendert das iframe dort offenbar intern bei einer eigenen, per JS nicht zuverlässig auslesbaren Auflösung, wodurch die `window.innerWidth/innerHeight`-basierte Skalierung ins Leere lief und weiterhin überlappende Karten + ein verschwundener Avatar auftraten. **Runde 2 (aktuell)**: `#stage-scale` und `fitStage()` komplett entfernt — keine JS-Messwerte mehr im Spiel. Stattdessen rein CSS-basiertes, fluides Layout: `#shake-root` bleibt bei `position:fixed; inset:0` (füllt dadurch nativ IMMER exakt den echten Viewport, browserintern berechnet, unabhängig von JS), und alle äußeren Anker-Container (`.top-left-stack`, `.goal-position`, `.avatar-position`, `.chat-stage-box`, `.branding-position`, `.soundtrack-position`) wurden von festen px-Werten (`top:24px` etc., berechnet für ein gedachtes 1920×1080-Raster) auf `vw`/`vh` umgestellt (z.B. `top:2.2vh; left:1.25vw`) — kleben dadurch direkt am jeweiligen Viewport-Rand, egal welche Auflösung/Seitenverhältnis Streamlabs Mobile tatsächlich liefert, ganz ohne Letterbox-Balken. Der Avatar-Breite (`210px` fix) wurde durch `clamp(120px, 11vw, 260px)` ersetzt, die drei HUD-Karten-`min-width`-Werte durch `min(240px, 34vw)` bzw. `min(200px, 30vw)`, damit sie bei sehr schmalen Ziel-Auflösungen mitschrumpfen statt sich zu überlappen (Inhalte mit `white-space:nowrap` können `min-width` trotzdem übersteuern, siehe unten). `SpraySystem`s `canvas.getBoundingClientRect()`-basierte Größe + `toLocal(x,y)`-Koordinatenumrechnung aus Runde 1 blieb unverändert bestehen (weiterhin korrekt, unabhängig davon ob eine Skalierungs-Transform im Spiel ist). Getestet: Playwright-Funktionssuite weiterhin gruen bei 1920×1080; realistische "falsche Desktop-Auflösung"-Szenarien (800×600, 1024×768, 1280×720, 1366×768, passend zur Nutzerbeschreibung) zeigen keine Überlappung und einen durchgehend sichtbaren Avatar, ohne Letterboxing - der Inhalt füllt den Rahmen jetzt randvoll. **Bekannte Grenze**: bei extrem schmalen Hochformat-Viewports (z.B. Handy-Portrait 390px breit) können Wanted-Level- und Goal-Karte durch nicht umbrechenden Text (`white-space:nowrap`) trotzdem kollidieren — aber ein Streamlabs-Mobile-Browser-Quellen-Rechteck für ein 16:9-Overlay ist praktisch nie Handy-Hochformat-schmal, daher bewusst nicht weiter optimiert. **Runde 3 (aktuell)**: Positionierung laut Nutzer-Live-Test jetzt korrekt (Karten kleben sauber in den Ecken, keine Überschneidungen), aber Streamlabs Mobile meldet dem iframe offenbar eine sehr geringe interne Auflösung, wodurch die (an sich korrekt vw/vh-verankerten) Widgets riesig wirkten und einen Großteil des Gameplays verdeckten. Fix: neue globale CSS-Variable `--ui-scale: 0.55` im `:root`, angewendet als `transform: scale(var(--ui-scale))` auf alle sechs äußeren Anker-Container (`.top-left-stack`, `.goal-position`, `.avatar-position`, `.chat-stage-box`, `.branding-position`, `.soundtrack-position`), jeweils mit passendem `transform-origin` auf die tatsächliche Anker-Ecke (top left / top right / bottom left / bottom right / center bottom), damit die Karten beim Schrumpfen an ihrer Ecke "kleben" bleiben statt zur Bildschirmmitte zu rutschen. Bei `.soundtrack-position` mit dem bereits vorhandenen `translateX(-50%)` zu `translateX(-50%) scale(var(--ui-scale))` kombiniert. Der Party-Popup (Alert-Overlay, bildschirmmittig) wurde bewusst NICHT mitskaliert - er ist transient (nur ~5s pro Alert sichtbar) und nutzt eine eigene, JS-gesteuerte Hydraulik-Transform-Kette; das Nutzer-Problem ("verdeckt fast das halbe Gameplay") bezog sich erkennbar auf die dauerhaft sichtbaren HUD-Ecken-Widgets. `--ui-scale` ist ein einzelner zentraler Regler - bei Bedarf (z.B. falls 0.55 immer noch zu groß/klein ist) reicht eine einzige Zahlenänderung im `:root`-Block. **Runde 4 (aktuell)**: `--ui-scale`/`transform: scale()` laut Nutzer-Live-Test der falsche Mechanismus - die Streamlabs-Mobile-Android-WebView rendert herunterskalierte Layer sichtbar unscharf/verpixelt (Text/Bilder wirken wie schlechte JPEGs), vermutlich weil Text/Bilder dort nicht bei der finalen (kleinen) Groesse neu rasterisiert werden, sondern die volle Groesse rendert und erst danach bitmap-artig verkleinert. Fix: `--ui-scale` und alle sechs `transform: scale()`/`transform-origin`-Paare komplett entfernt. Stattdessen JEDE Innen-Groesse (Font-Sizes, Paddings, Gaps, Border-Staerken, Breiten/Mindestbreiten, `--cut`-Clip-Path-Chamfer, Bullet-Hole-Radien, Avatar-`clamp()`-Grenzen, Chat-Card-Breite, Branding-/Soundtrack-Badge-Masse) direkt auf ~55% ihres Vorgaenger-Werts umgerechnet und als literale px/vw/vh-Werte hart eingetragen (z.B. `font-size: 13px` -> `7px`, `padding: 20px 26px 18px` -> `11px 14px 10px`) - die Engine zeichnet Schrift/Formen dadurch nativ in der kleinen Zielgroesse, kein nachtraegliches Herunterskalieren mehr noetig. Die sechs Anker-Container (`top/left/right/bottom` in vw/vh) blieben dabei absichtlich unangetastet (nur die `transform`-Zeilen entfernt) - exakt dieselbe Verankerung wie in Runde 3. `SpraySystem`/`toLocal()` aus Runde 1 unveraendert (war nie an `--ui-scale` gekoppelt). Der Party-Popup bleibt weiterhin bewusst in voller Groesse (siehe Runde 3). Getestet: Playwright-Funktionssuite weiterhin gruen; visuelle Pruefung bei 3x devicePixelRatio (simuliert hochaufloesenden Screenshot-Zoom) bestaetigt gestochen scharfen Text/Avatar ohne jede Unschaerfe. **Runde 5 (aktuell)**: Nutzer meldete nach Runde 4 zwei getrennte Folgeprobleme, beide in einem Rutsch behoben. (a) **RL-HUD-Kollisionsvermeidung**: Nutzer bestaetigte konkrete In-Game-HUD-Zonen von Rocket League (oben links = In-Match-Chat-Bubbles, oben Mitte = Tor/Zeit-Anzeige, unten rechts = Boost-Anzeige) — `.top-left-stack` (Wanted+Stats) umbenannt in `.left-edge-stack` und von fixer Ecken-Verankerung (`top:2.2vh; left:1.25vw`) auf vertikal zentrierte Positionierung am linken Rand umgestellt (`top:50%; left:1.25vw; transform:translateY(-50%)`), analog neue `.right-edge-stack` (buendelt Chat-Stage + Branding-Tag, vorher zwei getrennte Anker unten rechts) ebenfalls vertikal zentriert am rechten Rand. `.chat-card` dabei von fixer `187px`-Breite auf `92%`/`left:4%` relativ zur jetzt `position:relative`-Eltern-Box umgestellt (die `getBoundingClientRect()`-basierte Physik in `addChatMessage()` braucht dafuer keine JS-Aenderung). `.goal-position` (oben rechts), `.avatar-position` (unten links) und `.soundtrack-position` (unten Mitte) bewusst unangetastet gelassen, da vom Nutzer nicht als kollidierend gemeldet. Wichtige Einschraenkung: diese Zuordnung ist nur anhand der drei explizit genannten RL-Zonen hergeleitet (nicht jede denkbare HUD-Position im Spiel wurde bestaetigt) — taucht eine weitere Ueberschneidung live auf, muss gezielt nachjustiert werden. (b) **`--vu`-Fluid-Unit als Nachfolger von Runde 4s hart einprogrammierten px-Werten**: trotz entferntem `transform:scale()` meldete der Nutzer die Karten im echten Streamlabs-Mobile-Einsatz weiterhin als zu gross/unscharf — plausible Ursache: die Android-WebView liefert dem iframe intern einen kleineren Viewport als angenommen, wodurch fixe px-Werte (die nicht mit der Viewport-Breite mitschrumpfen) trotz allem ueberproportional gross wirken. Neue Wurzel-Variable `--vu: clamp(0.5px, 0.0521vw, 1px)` (ergibt exakt 1px bei 1920px Viewport-Breite, schrumpft proportional mit `vw` auf kleineren echten Viewports, nach unten bei 0.5px gedeckelt) — jeder bisher feste px-Wert im HUD (Font-Sizes, Paddings, Gaps, `--cut`-Chamfer, Bullet-Hole-Masse, Avatar-Glow-Inset, Chat-Card-Textur, Branding-/Soundtrack-Badge-Masse) auf `calc(var(--vu) * N)` umgestellt, mit demselben N-Faktor, der in Runde 4 bereits visuell austariert wurde. Wichtig: KEIN `transform:scale()` (das reine Bitmap-Nachskalierung nach der Rasterisierung ist und die Unschaerfe in Runde 3 verursachte) — `calc()`/`clamp()` werden schon beim Layout in konkrete Ziel-Groessen aufgeloest, Text/Formen werden nativ in der finalen Groesse gezeichnet und bleiben dadurch scharf, unabhaengig von der tatsaechlichen Viewport-Breite. (c) **Staerkere Avatar-Idle-/Reaktions-Animation** (zusaetzlicher Nutzer-Wunsch, nicht auf einen Fehlerbericht zurueckzufuehren): 2-Punkt-Wippen durch 4-Punkt-Keyframe ersetzt (Translation + wechselnde Rotation + leichtes Skalierungs-Atmen), Punch-Reaktion nutzt jetzt ein ueberschwingendes `cubic-bezier(0.34, 1.56, 0.64, 1)`-Easing mit staerkerem Transform (mehr Scale + Rotation) sowie einen groesseren/schnelleren Glow-Puls — identisch in `overlay.html` UND in `avatar.html` umgesetzt (Konsistenz zwischen Master-Szene und modularer Einzelseite, da beide denselben Baeren zeigen). (d) **Offline-Fallback-Panel fuer die Stats-Karte**: `api/stats.js` selbst wurde NICHT veraendert — die Function laeuft serverseitig auf Vercel, komplett unabhaengig vom lokalen Cloud-Gaming-Eingabeweg des Nutzers, die Theorie "Cloud-Gaming verursacht den Offline-Status" ist daher technisch unplausibel; die tatsaechliche Ursache (z.B. Tracker.gg-Downtime/Rate-Limiting) liess sich aus der Sandbox nicht verifizieren, da deren Netzwerk-Policy saemtliche ausgehenden Domains blockiert, inklusive der eigenen Vercel-Deployment-URL. Stattdessen die vom Nutzer selbst angebotene Alternative umgesetzt: neues `.stats-offline-panel` (Gold-Wordmark "D5" + Subline "DaN · DANGSXR1000") faellt per neuer `.is-offline`-Klasse auf `#statsContainer` automatisch ein, sobald `setStatsStatus('offline')` greift, versteckt dabei die 0/0/0%-Zeilen komplett (`display:none !important`), Status-Label-Text von woertlich "Offline" auf "DANGSXR1000" geaendert; der rote Status-Punkt bleibt bewusst als kleines technisches Diagnose-Signal unveraendert bestehen. Getestet: Playwright-Funktionssuite weiterhin gruen (auch nach Uebernahme der Avatar-Aenderungen in `avatar.html`), dedizierter Offline-Panel-Test (mockt `/api/stats` mit HTTP 500) bestaetigt Klasse/Panel-Sichtbarkeit/versteckte Stats-Zeile/Label-Text, Zoom-Screenshot bei 3x devicePixelRatio bestaetigt weiterhin scharfe Kanten nach der `--vu`-Umstellung, zusaetzliche Tiny-Viewport-Tests (480×270, 320×180) bestaetigen proportionale Verkleinerung ohne Ueberlappung. **Runde 6 (aktuell)**: Nutzer schickte einen Screenshot des Streamlabs-"Vorschaumodus" nach Runde 5 mit zwei optischen Nachbesserungen. (a) **Soundtrack-Badge zeigte abgeschnittenen Text** (z.B. "UNDTRACK: StreamBe" statt der vollen Zeile): Ursache war, dass `.soundtrack-text` UNBEDINGT scrollte (`padding-left:100%` + `animation: soundtrackMarquee` immer aktiv, unabhaengig davon ob der Text ueberhaupt zu lang fuer die Badge war) — bei `overflow:hidden` ohne Kanten-Fade zeigt jeder Schnappschuss zwangslaeufig eine hart abgeschnittene Teil-Ansicht. Fix: neue Funktion `setupSoundtrackMarquee()` (misst nach `document.fonts.ready`, damit die Google-Fonts-Nachlade-Verzoegerung die Breitenmessung nicht verfaelscht) vergleicht `textEl.scrollWidth` gegen `badgeEl.clientWidth` und setzt `.is-scrolling` nur, wenn der Text tatsaechlich breiter als die Badge ist — `padding-left:100%`/die Animation liegen jetzt in `.soundtrack-text.is-scrolling` statt auf der Basisklasse, passt der Text (wie der aktuelle Default-Text) hinein, bleibt er komplett statisch und vollstaendig lesbar stehen. Scrollt die Badge doch (z.B. bei einem sehr langen `?track=`-Wert), sorgt ein neuer `mask-image`-Weichzeichner an `.soundtrack-badge.is-scrolling` (horizontaler Verlauf transparent→deckend→transparent an den Raendern) dafuer, dass Text weich rein-/rausblendet statt hart abgeschnitten zu wirken. Bei Resize erneut gemessen (Event-Listener). (b) **Wanted-Level- und Stats/Rang-Karte zu zwei getrennten Boxen mit Luecke** (`.left-edge-stack` stapelte zwei unabhaengige `.shimmer-frame > .hud-card`-Bloecke): auf Nutzerwunsch ("kompakter und harmonischer als ein gemeinsamer Block") zu EINER gemeinsamen Karte zusammengefuehrt — neue Modifier-Klasse `.hud-card-merged` traegt jetzt Padding/Min-Width fuer eine einzelne `.shimmer-frame > .hud-card.hud-card-merged`, `#wantedContainer`/`#statsContainer` (IDs unveraendert, JS/CSS wie `setStatsStatus()`s `classList.toggle('is-offline', ...)` und die `.stats-card.is-offline`-Selektoren brauchten dadurch keine Anpassung) sind jetzt nur noch interne `display:flex;flex-direction:column`-Abschnitte OHNE eigene Kartenoptik, getrennt durch einen neuen `.hud-divider` (duenne Rot→Gold→Rot-Verlaufslinie). Dabei auch die vorher doppelt vorhandenen `.tape-corner`/`.hud-watermark` (je einmal pro Karte) auf je EIN Exemplar fuer die gesamte gemergte Karte reduziert (Watermark jetzt einheitlich "DANGSXR1000" statt vorher "DAN" auf der Wanted-Karte + "DANGSXR1000" auf der Stats-Karte). `.goal-card` (Follower-Ziel, oben rechts) unveraendert, war nicht Teil der Nutzer-Kritik. Getestet: volle Playwright-Funktionssuite weiterhin gruen, dedizierte Marquee-Checks bestaetigen `.is-scrolling` bleibt aus beim Default-Text (195px Text in 217px Badge) und schaltet sich bei einem absichtlich ueberlangen Test-Track-Namen zu, dedizierter Struktur-Check bestaetigt genau 1 `.shimmer-frame`/1 `.tape-corner`/1 `.hud-watermark`/1 `.hud-divider` in der gemergten Karte, Offline-Panel-Test weiterhin gruen nach der Restrukturierung, 3x-devicePixelRatio-Zoom-Screenshot bestaetigt weiterhin scharfe Kanten.
- **`overlay.html`** (All-In-One Master-Szene): alle Module auf einer 1920×1080-Browser-Quelle zusammengeführt, Layout + gemeinsame tmi.js-Verbindung siehe Dateistruktur oben. Per Playwright visuell (Screenshots bei voller Auflösung, Ruhezustand + live während eines Alerts) und funktional getestet (Rang/Stats-Rendering, Wanted-Level-EventSub-Integration, gemeinsame Chat-Verbindung löst Chat-Karte + Command + Avatar-Reaktion gleichzeitig aus).
- **UI-Re-Redesign "Urban Blackout"**: kompletter Farb-/Formen-Umbau von `goal.html`, `chat.html` und den Hood-Widgets in `overlay.html` weg vom braun-zerrissenen "Hood/Ghetto"-Look (wirkte laut DaN nach "Schrottplatz/Western") hin zu einem tiefschwarzen, hartkantigen Street-/Tech-Panel-Design (Details siehe Styling-Guides). Alle drei Dateien nacheinander umgesetzt und einzeln per Playwright-Screenshot verifiziert (Ruhezustand + Alert-Popup), Funktionstests (Wanted-Level, Chat-Karte+Command+Avatar-Reaktion, Rang-Rendering) liefen unverändert grün — reine CSS-Umstellung, keine JS-/Selector-Änderungen.
- **`start.html`** (neue "Starting Soon"-Szene): siehe Dateistruktur oben für die volle Beschreibung. Erste Version einer Canva-Recherche vorausgegangen (Suche nach "Bär freistellen" u.ä. in den Canva-Designs des Nutzers) — auf Nutzer-Feedback hin verworfen, da explizit NUR das direkt in der Nachricht angehängte finale Bärenkopf-Bild verwendet werden sollte, keine alten/fremden Canva-Dokumente. **Bärenkopf-Bild-Bergungsaktion**: nach zwei gescheiterten Upload-Versuchen (siehe Historie Punkt 14) konnte der Nutzer keine echte transparente PNG-Datei liefern — statt eines dritten Upload-Zyklus wurde `bear_avatar.png` (JPEG mit eingebranntem Schachbrett-Hintergrund) per Python/Pillow-Skript direkt in der Sandbox freigestellt: Luminanz-Histogramm zeigte eine perfekt saubere Lücke zwischen Bär-Tönen (max. ~120) und Schachbrett-Tönen (min. ~210) — Schwelle mittig in diese Lücke gelegt (Luminanz 140-195 als weicher Uebergang, plus Saettigungs-Check gegen die roten Glow-Akzente), `ImageFilter.MedianFilter` gegen JPEG-Block-Rauschen, `ImageFilter.MinFilter` (leichte Erosion) gegen einen hellen Kontaminations-Saum an den Kanten, manueller Crop auf die sichtbare Silhouette. Ergebnis visuell gegen den echten `--asphalt`-Bühnenhintergrund geprüft (Restrauschen bei niedriger Deckkraft auf Schwarz praktisch unsichtbar) und als `assets/images/bear_head_lowpoly.png` (714×804, echtes RGBA) gespeichert — kein weiterer Upload vom Nutzer nötig. Danach die vom Nutzer gewünschten "5-Minuten-lebendig"-Effekte ergänzt (Augen-Glow, Glitch-Ausreisser, simulierter Audio-Visualizer, rotierende Status-Meldungen). **Runde 2**: diese komplette erste Version wurde durch die Umsetzung eines vom Nutzer in Claude Design gebauten Prototyps ersetzt (Import-Weg, Umfang der Übersetzung, Typografie-Entscheidung, siehe Dateistruktur oben für die volle Beschreibung) — die Bärenkopf-Bergungsaktion und die Grundidee "Szene darf nicht starr wirken" blieben dabei relevant/übernommen, nur die konkrete Umsetzung der Lebendigkeits-Effekte kommt jetzt 1:1 aus dem Design statt aus eigener Konzeption. **Runde 3 (aktuell)**: Nutzer-Korrektur — das aus dem Design-Prototyp übernommene "HYPE LEVEL"-HUD (5 Sterne, an `goal.html`s Wanted-Level-`localStorage` gekoppelt) hat laut Nutzer auf dem Start-Bildschirm "absolut nichts zu suchen" und wurde restlos entfernt (CSS-Block, HTML-Markup, `initWantedStars()`, alle `sideHud`/`starEls`-Referenzen samt Stern-Twinkle-/HUD-Breathing-Logik in `animFrame()`). Da der Nutzer in seiner Liste der Elemente, die sich den frei gewordenen Platz teilen sollen, auch "VIEWERS" nicht erwähnte, wurde bewusst das **gesamte** `.side-hud`-Panel entfernt (nicht nur die Sterne) — inklusive des ohnehin nicht-funktionalen "VIEWERS: —"-Platzhalters, da ein einzelner unsymmetrischer Rest-Block dem ausdrücklichen Ziel "sauber, symmetrisch, minimalistisch" widersprochen hätte; sollte VIEWERS eigentlich erhalten bleiben, ist das eine gezielte Nachjustierung. Der zentrale Bärenkopf wurde als Ausgleich für den frei gewordenen Platz vergrößert (`.bear-wrap`-Breite von `calc(var(--vu) * 460)` auf `calc(var(--vu) * 500)`), wodurch die verbleibenden Elemente (Twitch-Leiste, Bär, Countdown+Status-Ticker, Soundtrack-Leiste) sich jetzt als eine einzige vertikal zentrierte Spalte anordnen statt mit einem asymmetrischen Seiten-Panel zu konkurrieren. Die beiden Status-Ticker-Meldungen, die inhaltlich auf das entfernte Konzept verwiesen ("LOADING HYPE METER...", "CALIBRATING WANTED SCANNER..."), wurden durch thematisch neutrale Meldungen ersetzt (u.a. "SYNCING CHAT UPLINK...", "ARMING SOUNDTRACK MODULE..."). Die `--vu`-Fluid-Unit-Skalierung und die dadurch erzielte Schärfe blieben unangetastet. Getestet: Playwright bestätigt `.side-hud`/`.wanted-star` existieren nicht mehr im DOM, Bär-Wrap-Breite ist jetzt `500px`, alle verbleibenden Elemente rendern weiterhin fehlerfrei, Screenshot-Kontrolle bestätigt das gewünschte symmetrische Gesamtbild. **Runde 3 (aktuell)**: kompletter Neuaufbau nach einem zweiten Claude-Design-Export ("Twitch Starting Soon") — siehe Dateistruktur oben für die volle Beschreibung. Unter Zeitdruck entstanden (Nutzer wollte noch am selben Tag live gehen) — Extraktion, Bildwiederverwendung und Umsetzung in einem Zug ohne Zwischen-Rückfragen, da alle offenen Architekturfragen (Mikro-Strategie, "Spenden"-Mapping, Musik-Anbindung) bereits kurz zuvor für `in-game.html` geklärt worden waren und hier identisch galten.
- **`in-game.html`**: siehe Dateistruktur oben für die volle Beschreibung der aktuellen Runde 2. Architektur-Rückfragen vor Baubeginn der ersten Runde 1 (Beziehung zu `overlay.html`, Bären-Asset-Wahl, Mikro- vs. Chat-Reaktivität, "Spenden"-Datenquelle) blieben zweimal unbeantwortet — mit klar dokumentierten, empfohlenen Standardannahmen weitergebaut: zusätzliche Alternativ-Szene statt Ersatz, Low-Poly-Kopf-Bär statt Ganzkörper-Avatar, Chat/Events primär + Mikro als Bonus-Pfad, "Spenden" = Bits/Cheers. **Runde 2 (aktuell)**: Nutzer lieferte einen echten Claude-Design-Export nach ("Twitch In-Game HUD") — die selbst erfundene "Vintage Hip-Hop/Gold-Bling"-Lederpalette aus Runde 1 traf den tatsächlichen Nutzer-Wunsch nicht, komplett durch das im Export vorgefundene Design ersetzt (Details/Extraktionsweg siehe Dateistruktur oben). Vier zusätzliche Architektur-Entscheidungen dabei per `AskUserQuestion` bestätigt: Mikro bleibt Bonus-Pfad (Empfehlung angenommen), "Rocket League scharf stellen" = echtes RL-Stats-Widget geplant (in dieser Runde aus Zeitgründen noch nicht umgesetzt, siehe Offene Punkte), Hype-Level-Key bewusst NICHT mit `dangsxr_wanted_points` vereinheitlicht, Musik bleibt zunächst beim `?track=`/`?artist=`-Parameter (Nutzer hat Spotify per MCP verbunden und sich für eine spätere echte Live-Anzeige entschieden, siehe Offene Punkte). Unter Zeitdruck entstanden (Nutzer wollte noch am selben Tag live gehen) — beide Design-Dateien direkt selbst analysiert/extrahiert statt über Recherche-Agents (schneller), Umsetzung in einem Zug ohne weitere Zwischen-Rückfragen. **Runde 4 (aktuell, Layout-Fix nach echtem Live-Screenshot)**: Nutzer schickte einen Screenshot des echten Streams — Bär, Ticker und die in Runde 3 ergänzte RL-Stats-Karte waren als ein hoher Block oben links gestapelt und überlappten sichtbar die echte Rocket-League-Lobby-Menüliste (Item-Shop/Rocket-Pass/Einstellungen/Verlassen). Bär steht jetzt freistehend unten mittig (`.bear-position`, kollidiert dort weder mit Hype-Karte noch mit Chat/Event-Karten) und hat ein permanentes Idle-Wippen (`.bear-bob`, 1:1 Keyframe aus `avatar.html`) statt vorher zwischen Events komplett bewegungslos zu sein — echte Musik-/Mikro-Reaktivität bleibt technisch nicht verlässlich verfügbar (Browser-Quelle hat keinen Zugriff auf die von Streamlabs gemischte Musik, Mikro-Zugriff bestätigt blockiert), das permanente Wippen ist die ehrliche Alternative dazu, zusätzlich zur weiterhin bestehenden echten Reaktion auf Chat/Events. Ticker + RL-Stats-Karte wurden aus dem Eck-Block gelöst und in einen neuen, vertikal zentrierten `.left-info-stack` verschoben (gleiches Prinzip wie `overlay.html`s `.left-edge-stack`). RL-Stats-Karte zeigte laut Nutzer "0/0/0%" ohne erkennbaren Grund (echte Session mit 0 Matches vs. API-Fehler nicht unterscheidbar) — hat jetzt einen Text-Status neben dem Punkt ("Verbinde…"/"Live"/"Cached"/"Offline"), Platzhalter-Werte ("–" statt "0") bis der erste echte Session-Delta vorliegt, und ein Offline-Panel (`.is-offline`, gleiches Muster wie `index.html`/`overlay.html`) statt bei einem echten API-Ausfall stumm bei 0/0/0% zu bleiben. Getestet: Playwright bestätigt keine Überlappung des Bären mit Hype-/Chat-/Event-Karten oder der Bottom-Bar, Idle-Animation aktiv, `?test=react` löst weiterhin die Action-Pose aus, Offline-Panel schaltet sich korrekt frei (im Sandbox-Netzwerk erwartungsgemäß dauerhaft "Offline", da `/api/stats` unerreichbar ist). **Runde 5 (aktuell)**: Nutzer-Feedback auf einen weiteren Live-Screenshot — der Bär sollte "nicht in einem Kasten stehen". Der komplette Chrome-Rahmen/dunkle Kartenhintergrund/Eckklammern/LIVE-Badge aus Runde 4 (`.bear-outer`/`.bear-inner`/`.bear-corner`/`.bear-live-badge`) wurde ersatzlos entfernt — der Bär besteht jetzt nur noch aus den beiden Bild-Layern selbst mit rotem Glow-Drop-Shadow statt einem umschließenden Rahmen, dadurch auch deutlich größer darstellbar (`.bear-position` von 300×230 auf 320×460, da nicht mehr durch eine Kartenbox begrenzt). Zusätzlich zwei offene Nutzer-Anliegen aus derselben Nachricht: (a) RL-Stats-Karte zeigte im Screenshot weiterhin "Offline" — da `api/stats.js` serverseitig auf Vercel läuft und aus der Sandbox nicht erreichbar ist (HTTP 403 auf Proxy-Ebene, mit `WebFetch` bestätigt), ließ sich das nicht aus der Sandbox diagnostizieren; Nutzer wurde gebeten, `https://stream-hub-three-gold.vercel.app/api/stats` direkt im mobilen Browser (nicht in der App) zu öffnen und die rohe JSON-Antwort zu teilen, um zwischen einem echten Tracker.gg-Problem und etwas anderem zu unterscheiden — noch offen. (b) Echte Musik-/Stimm-Reaktivität erneut eingefordert ("super Qualität", "erkennt meine Stimme oder die Musik") — Mikro-Zugriff bleibt technisch blockiert (siehe Runde 4), für echte Musik-Reaktivität wurde als einzig realistischer Weg eine künftige Kopplung an Spotifys Track-Tempo/BPM-Daten vorgeschlagen (sobald die in Offene-Punkte-Item 16 geplante Spotify-Integration existiert) statt echter Audio-Analyse, die technisch nicht zugänglich ist — noch nicht umgesetzt, da die Spotify-Integration selbst noch aussteht. Der weiterhin sichtbare Overlap von Ticker/RL-Stats-Karte mit dem Rocket-League-Lobby-Menü aus dem Nutzer-Screenshot wurde in dieser Runde bewusst nicht nachjustiert (vom Nutzer diesmal nicht explizit angesprochen, exakte kollisionsfreie Zone für diesen speziellen Lobby-Screen noch unklar). Getestet: Playwright bestätigt Kasten-Elemente sind aus dem DOM entfernt, neue Bär-Größe rendert ohne Überlappung mit Hype-/Chat-/Event-Karten/Left-Info-Stack/Bottom-Bar, `?test=react` funktioniert weiterhin, keine JS-Fehler. **Runde 6 (aktuell)**: Nutzer testete `in-game.html` diesmal direkt im mobilen Browser (statt in der Streamlabs-App) und meldete zwei Dinge: Mikro-Reaktivität funktioniert dort tatsächlich ("Der Bär reagiert auf meine Stimme") — konsistent mit der bereits dokumentierten Erkenntnis, dass `getUserMedia()` nur in der Streamlabs-Mobile-Android-**WebView** blockiert ist, in einem normalen Mobile-Browser aber funktioniert; kein neuer Fund, sondern Bestätigung des bekannten Verhaltens. Zweitens: der Bär sei "in der Mitte und zu groß" — `.bear-position` war seit Runde 5 auf 320×460 (ohne Kastenbegrenzung deutlich gewachsen), das nimmt bei einer 1920×1080-Referenz gut 43% der Bildschirmhöhe ein. Größe auf 200×288 reduziert (Seitenverhältnis beibehalten), Position (horizontal zentriert, unten) unverändert gelassen, da vom Nutzer nicht als falsch benannt. **Wichtiger Hinweis für künftige Positionierungs-Wünsche an diesem Bären**: seit der Modularisierungs-Runde (siehe `avatar.html`/die sechs Einzel-Widgets oben) gibt es mit `avatar.html` bereits eine eigenständige, frei über Streamlabs' Transform-Panel skalier- und positionierbare Bären-Quelle, die genau für diesen Zweck gebaut wurde und weiteres Herumraten an CSS-Werten für den fest im `in-game.html`-Bündel verankerten Bären überflüssig macht. Getestet: Playwright bestätigt neue Bär-Maße (200×288 statt 320×460), keine JS-Fehler, Screenshot-Kontrolle bestätigt keine Überlappung mit den übrigen Karten.

**Offen — nächste Schritte, in etwa dieser Reihenfolge:**
1. ~~`assets/images/bear_head_lowpoly.png` fehlt noch~~ **Erledigt (auf ungewoehnlichem Weg).** Zwei Upload-Versuche des Nutzers scheiterten am selben Grundproblem wie beim allerersten Avatar-Bild (siehe Punkt 14 unten): `bear_avatar.png` landete im Repo-Root und war trotz `.png`-Endung tatsächlich ein **JPEG ohne Alpha-Kanal** — das Schachbrett-Transparenz-Symbol aus dem Editor des Nutzers war als echte graue/weiße Karo-Pixel fest eingebrannt. Da ein dritter manueller Re-Export absehbar nicht praktikabel war, wurde das Bild stattdessen **direkt in der Sandbox per Python/Pillow freigestellt** (Luminanz-Schwellwert-Keying in einer sauberen Luecke der Farbverteilung zwischen Bär- und Schachbrett-Tönen, Median-Filter gegen JPEG-Rauschen, leichte Erosion gegen Kanten-Kontamination) — Ergebnis als `assets/images/bear_head_lowpoly.png` (714×804, echtes RGBA) gespeichert, visuell gegen den echten Bühnenhintergrund geprüft. Kein weiterer Upload nötig.
2. **Live-Validierung**: Overlays noch nie vollständig gegen die echte Streamlabs-Mobile-App getestet (nur teilweise per Screenshot bestätigt: `chat.html`-Demo-Modus rendert korrekt; `goal.html`-Widget erschien dabei am oberen Rand abgeschnitten — vermutlich Browser-Source-Breite/Höhe in Streamlabs Mobile nicht auf 1920×1080 gesetzt, muss geprüft werden). Jetzt zusätzlich zu prüfen: Mikrofon-Berechtigungsdialog für das tmi.js-basierte Chat-Commands-Script wird nicht benötigt (reiner Lesezugriff), aber CDN-Erreichbarkeit von `cdn.jsdelivr.net` in der echten Umgebung sollte einmal beobachtet werden (in der Sandbox blockiert, auf einem normalen Android-Tablet aber ohne bekannten Grund zur Sorge). Neu zu prüfen: `avatar.html`s Idle-/Chat-reaktive Animation live im Stream (Mikro-Bonus wird in Streamlabs Mobile bestätigt NICHT funktionieren, das ist jetzt erwartetes Verhalten, kein Fehler). **`overlay.html` nutzt jetzt `--vu`-Fluid-Sizing statt fester px-Werte** (siehe "Hotfix-Historie: overlay.html-Layout", Runde 5 oben) — der neue Ansatz braucht keine zuverlässigen `window.innerWidth/innerHeight`-Messwerte und soll gleichzeitig das Größen- UND das Unschärfe-Problem aus den Runden 3+4 lösen, ist aber noch nicht am echten Gerät bestätigt - genau das ist der nächste nötige Schritt. Live prüfen, ob wirklich alle Zonen (Wanted-Level+Stats **linker Rand, vertikal zentriert**, Follower-Goal oben rechts, Avatar unten links, Chat+Branding **rechter Rand, vertikal zentriert**, Soundtrack-Badge unten mittig) sauber sichtbar sind und nichts mehr überlappt. **Wichtigster neuer Check (Runde 5)**: ob die neue Randpositionierung tatsächlich kollisionsfrei mit Rocket Leagues eigenem In-Game-HUD ist (In-Match-Chat oben links, Tor/Zeit oben Mitte, Boost unten rechts müssen frei bleiben) — die Zuordnung „linker/rechter Rand vertikal zentriert = sicher" ist nur durch Ausschluss der drei vom Nutzer genannten Zonen hergeleitet, nicht am echten Spiel bestätigt. **Zusätzlich für `in-game.html`**: dessen Layout platziert bewusst NEUE Elemente in bislang von `overlay.html` gemiedenen Zonen (Bär oben links, Ticker unten breit) — die dort gewählten Einrückungen (`top:9vh`/`left:1.5vw` für den Bären, `left:14vw`/`right:22vw` für die Ticker-Leiste) sind reine Sandbox-Annahmen, ebenfalls noch nie gegen das echte Spiel-HUD getestet.
3. Bildschirmfreigabe/App-Capture in Streamlabs Mobile aktivieren (war beim letzten Test noch aus, deshalb schwarzes Vorschaubild).
4. **Soundtrack by Twitch** aktivieren — kostenlose, lizenzfreie Hintergrundmusik, DMCA-sicher, kein Code nötig. Wichtig für DaN als Nicht-Mic-Streamer.
5. Twitch-Erweiterung "Sound Alerts" deaktivieren (Risiko: doppelter Ton, da `goal.html` jetzt eigene Alert-Sounds spielt) — Empfehlung ausgesprochen, noch nicht bestätigt/umgesetzt.
6. Texte aus `PANEL_CONCEPT.md` (Bio, Live-Benachrichtigung, 5 Panel-Texte) sind fertig formuliert, aber noch nicht im echten Twitch-Dashboard eingetragen.
7. 5 Panel-Grafiken produzieren (Spec steht in `PANEL_CONCEPT.md`).
8. Neues Offline-/Video-Player-Banner produzieren (ersetzt das alte Neon-Cyberpunk-Bild) — ein erster Design-Philosophie-Entwurf ("Scarred Chrome") wurde begonnen, aber noch nicht fertiggestellt.
9. Profilbild-Konzept produzieren (Vorschlag: Marken-Badge/Wortmarke statt Foto, da kein bearbeitbares Foto vorliegt).
10. Social-Media-Handles (Twitter/X, Instagram, TikTok, Discord) von DaN festlegen lassen — Socials-Panel ist strukturell fertig, aber inhaltlich leer.
11. `index.html`/`branding.html` optisch an den Urban-Blackout-Look angleichen (aktuell bewusst unverändert im alten Glassmorphism-Look, siehe Styling-Guides).
12. Phase 3 der Roadmap (Chat-TTS, Twitch-Schedule einrichten — Channel-Points-Rewards/Chatbot-Commands sind durch Modul 1 teilweise bereits vorweggenommen).
13. Affiliate-Status abwarten (0/10 Abonnenten-Punkte) — Emote-/Sub-Badge-Redesign erst danach sinnvoll (Stufe 2/3 sind vorher gesperrt).
14. ~~`assets/images/avatar.png` Bild-Status~~ **Erledigt.** Verlaufs-Notiz: (a) erstes Bild hatte ein abgeschnittenes Text-Fragment unten links (vermutlich ein gekapptes KI-Wasserzeichen), (b) zweiter Upload-Versuch behauptete transparenten Hintergrund, war aber laut `file`-Check ein reines RGB-PNG ganz ohne Alpha-Kanal mit sichtbar schwarzem Hintergrund — nicht selbstständig repariert (haette die Baer-Linienart mitzerstoert), sondern dem Nutzer gemeldet, woraufhin er es selbst wieder loeschte. (c) Der Nutzer hat daraufhin zwei neue, diesmal echt transparente RGBA-Bilder hochgeladen (`avatar_idle.png` + `avatar_action.png`, jeweils per `file`-Befehl auf echten Alpha-Kanal verifiziert) und damit gleich das Konzept von einem auf zwei Bild-Zustaende erweitert (siehe Modul 3 oben). Wie bei jedem bisherigen Upload landeten die Dateien ueber die GitHub-Web-UI im Repo-**Root**, nicht in `assets/images/` (trotz gegenteiliger Nutzerangabe) — beim naechsten Bild-Upload vorsichtshalber immer mit `git status`/`ls` nach dem Pull gegenchecken, wo die Datei tatsaechlich gelandet ist.
15. ~~Echtes Rocket-League-Stats/Rang-Widget für `in-game.html`~~ **Erledigt (Runde 3, direkt nach dem ersten Live-Einsatz nachgereicht).** Neue RL-Stats-Karte, wiederverwendet dieselbe `fetchStats()`-Session-Delta-Logik wie `index.html`/`overlay.html` gegen die bestehende `api/stats.js` (kein neuer Server-Code), im aktuellen Chrome/Gold-Bling-Kartenstil. Live/Cached/Offline-Status jetzt als Punkt **plus Text-Label** (seit Runde 4, siehe oben) statt nur eines kleinen Punkts, inkl. Offline-Fallback-Panel und `.left-info-stack`-Positionierung (vertikal zentriert am linken Rand, nicht mehr fest oben links neben dem Bären).
16. **Live-Spotify-Ticker** — Nutzer hat Spotify per MCP mit der Claude-Code-Session verbunden und sich bewusst (trotz angesprochenem DMCA-Risiko bei Spotify-Wiedergabe im Stream, siehe Punkt 4 oben zu "Soundtrack by Twitch") für eine echte Live-Anzeige des aktuell laufenden Songs im Ticker entschieden. Braucht eine neue Server-Integration (`api/spotify-now-playing.js`, analog `api/stats.js`) plus eine einmalige Spotify-Developer-App-Registrierung + OAuth-Refresh-Token seitens des Nutzers (analog `TWITCH_REFRESH_TOKEN`-Setup) — noch nicht begonnen, `?track=`/`?artist=`-Parameter bleibt bis dahin der produktive Weg. **Neue Idee (in-game.html Runde 5)**: da echte Mikro-/System-Audio-Analyse in der Browser-Quelle technisch nicht zugänglich ist (siehe `avatar.html`), aber der Nutzer wiederholt eine "reagiert auf die Musik"-Bär-Animation wünscht, könnte diese Integration zusätzlich Spotifys Track-Tempo/BPM-Daten abgreifen und darüber Tempo/Intensität des Bär-Idle-Wippens steuern — ein technisch ehrlicher Ersatz für echte Audio-Reaktivität (kein Fake-Zufallswippen, sondern an den tatsächlich laufenden Song gekoppelt), aber abhängig von der noch ausstehenden Spotify-Integration selbst.
17. ~~Ticker/RL-Stats-Karte in `in-game.html` kollidiert im Rocket-League-Lobby-/Pausenmenü~~ **Strukturell gelöst (siehe Modul „Frei positionierbare Einzel-Widget-Quellen" oben).** Laut weiterem Live-Screenshot (Nachspiel-Ergebnisliste, DRITTER unterschiedlicher RL-Bildschirm nach Lobby-Menü und Ingame-HUD) hat Rocket League schlicht keinen Bereich, der auf JEDEM eigenen Bildschirm frei bleibt — weiteres Koordinaten-Raten im CSS konvergierte nicht. Statt einer vierten Positions-Vermutung wurden `rl-stats.html`, `status-ticker.html` und die vier weiteren Widgets als eigene, frei per Streamlabs-Transform-Panel positionierbare Quellen ausgekoppelt — DaN kann sie jetzt selbst dorthin ziehen, wo auf seinem Gerät tatsächlich Platz ist, statt dass das im Code geraten wird. Das `in-game.html`-Bündel selbst behält seine aktuelle (weiterhin potenziell kollidierende) Positionierung als einfachere "Ein-Link"-Alternative für alle, die die Feinjustage nicht brauchen.

**Tech-Stack-Entscheidung (bereits getroffen, nicht neu diskutieren):**
Streamlabs Mobile (einzige praktikable Broadcast-Software für das 100% mobile Cloud-Setup ohne PC), GitHub + Vercel (Code/Deploy-Pipeline) und Claude Code (Entwicklung) sind der gesetzte Kern-Stack. StreamElements/Cloudbot (Chat-TTS + Commands) und Discord (Community-Hub) sind für Phase 3/4 vorgesehene Ergänzungen. Gemini ist kein Teil des Kern-Stacks (höchstens optional für Content-Brainstorming). OBS ist nicht relevant (Desktop-only, Setup ist 100% mobil). **Keine externe Datenbank** (kein Vercel KV, kein Redis/Upstash) — bewusste Entscheidung für maximale Einfachheit/Wartungsfreiheit. Persistenter Zustand, der einen Server-Roundtrip bräuchte (z.B. Wanted-Level-Fortschritt), läuft stattdessen über `localStorage` im Browser der jeweiligen Browser-Quelle; konfigurierbare Werte (z.B. der Soundtrack-Badge-Text) über URL-Query-Parameter nach dem bewährten `?channel=`-Muster aus `chat.html`.

**Kernstrategie-Prinzip** (aus der Diskussion um Engagement ohne Kamera/Mikrofon): Das Overlay ersetzt die Mimik (audio-reaktive Effekte statt Gesichtsausdruck), der Chat ersetzt die Stimme, künftiges Chat-TTS gibt den Zuschauern das Gefühl gehört zu werden, Channel-Points-Rewards, die direkt Overlay-Effekte auslösen, ersetzen Blickkontakt/Interaktion. Der Idle-Ambient-Layer wurde eingeführt, weil die Overlays vorher nur bei Alerts lebendig wirkten (~2% der Sendezeit) — die übrigen >95% sahen statisch aus, was laut DaN direkt zum Wegklicken bei anderen Streams führt.

## Proactive Code & Feature Audit Directive

- Scanne bei neuen Aufgaben das Repository auf ungenutzten Code, Sicherheitslücken und Performance-Engpässe.
- Schlage proaktiv neue Widgets, Design-Verbesserungen oder API-Erweiterungen vor, die zu diesem Streamer-Setup passen.
